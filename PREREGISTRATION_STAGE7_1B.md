# PREREGISTRATION — QUADC5 Stage 7.1b: сертифицированная вилка для n = 11

Запечатывается **2026-08-24**, до первого прогона. Хэши — в
`PREREGISTRATION_STAGE7_1B.sha256`; коммит печати предшествует любому коммиту с
результатами стадии. Действуют стандартные правила §0 `PREREGISTRATION.md`.

---

## 0. Что закрыто и не переоткрывается

Замкнутой формы для Δ_max(11) нет. PSLQ на **495 честных цифрах**, степени 2…48,
**ноль кандидатов**; границы высоты — от 10¹⁵⁸ при степени 2 до 10⁹ при степени 48,
в частности 10⁹⁵ при степени 4 (`results/report_7a_pslq_deep.json`).

**Углубление до 1000 цифр не делается.** Оно усилило бы уже достаточное отрицание —
границу 10⁹⁵ до примерно 10¹⁹⁵ при степени 4 — и стоило бы порядка шести часов счёта.
Отрицательный результат от этого не изменится: замкнутой формы как не было, так и не
будет. Решение и его причина фиксируются здесь, до счёта, чтобы потом не выглядеть
задним умом.

## 1. Цель

Перевести Δ_max(11) из численного в доказанное, **не имея замкнутой формы**: предъявить
явный рациональный интервал, в котором значение лежит, и доказать, что второе место
лежит строго ниже этого интервала.

## 2. Метод, зафиксированный до счёта

Численность участвует только в предложении матриц. Вердикт несёт точная арифметика в ℚ;
плавающая точка в него не входит.

### 2.1 Примал — нижняя граница

Граф `` J?`D@pgd?{? ``, n = 11, α = 4.

1. X\* берётся из `quadc5.hiprec.refine` при dps = 300 (значения на рёбрах там порядка
   10⁻²⁹⁰, то есть заведомо ниже любого используемого знаменателя).
2. Каждый элемент округляется до рационального со знаменателем D: X̃ = round(D·X\*)/D.
3. Точно симметризуется: X̃ ← (X̃ + X̃ᵀ)/2.
4. Точно зануляется на всех рёбрах.
5. Регуляризация: M = X̃ + εI, где ε = c/D. Прибавление εI сохраняет и симметрию, и
   нули на рёбрах (I диагональна), и только увеличивает собственные значения.
6. Нормировка: X = M / Tr(M) — деление на положительное рациональное; сохраняет PSD,
   даёт Tr X = 1 ровно и не трогает нули на рёбрах.
7. **Точная** проверка PSD двумя независимыми методами: пивотированное дополнение Шура
   и **все** 2¹¹ − 1 = 2047 главных миноров. Методы обязаны согласиться.
8. L = 1ᵀX1 — точное рациональное. Тогда ϑ(G) ≥ L, и это доказано.

### 2.2 Дуал — верхняя граница

1. B\* берётся оттуда же (поле `M` результата `refine`).
2. Элементы на рёбрах округляются до знаменателя D; диагональ и не-рёбра равны 1 точно
   по построению структуры.
3. Для рационального u проверяется **точно**, теми же двумя методами, что uI − B̃ ⪰ 0.
   Если да, то λ_max(B̃) ≤ u и ϑ(G) ≤ u.
4. u сужается делением пополам по рациональным до фиксированной здесь глубины 60
   шагов или до ширины 10⁻¹² — что наступит раньше.
5. U — наименьшее прошедшее u. Тогда ϑ(G) ≤ U.

### 2.3 Лестница знаменателей

D пробегает **10⁶, 10⁹, 10¹², 10¹⁵, 10¹⁸** в этом порядке; c пробегает 1, 2, 4, 8, 16
внутри каждого D. Порядок фиксирован здесь и по ходу не меняется. Отчитывается, на
каком шаге лестницы гейт был взят и сколько шагов потребовалось.

### 2.4 Второе место

Граф ранга 2, `` J?\`@f?kUDG_ ``, Δ ≈ 0.7546860747. Для него строится **только** дуал —
верхняя граница U₂ по той же процедуре. Полная вилка не нужна.

## 3. ГЕЙТ

**Ширина доказанного интервала U − L должна быть меньше разрыва до второго места,
0.0202024631.** Иначе цель не достигнута: интервал не отделяет лидера от преследователя.
При провале — следующий шаг лестницы, с записью числа итераций.

## 4. ЗАПЕЧАТАННЫЕ ПРЕДСКАЗАНИЯ

**P-B1.** Гейт берётся на **первом** шаге лестницы, D = 10⁶: ширина интервала там
окажется меньше 10⁻³, то есть на порядки уже разрыва 0.0202.

**P-B2.** На шаге D = 10¹² ширина окажется **меньше 10⁻⁸**, то есть интервал зафиксирует
Δ_max(11) не менее чем на восьми десятичных знаках.

**P-B3.** Оба метода проверки PSD согласятся в **каждом** испытании. Расхождение
означало бы ошибку в одном из них, а не свойство матрицы.

**P-B4.** U₂ < L строго, и зазор L − U₂ попадёт в **(0.0190, 0.0210)**.

**P-B5.** Регуляризация обойдётся дёшево: потеря нижней границы относительно численного
значения составит порядка 40ε, то есть при D = 10¹² — меньше 10⁻⁹.

### Что считается подтверждением
Все пять сбылись.

### Что считается провалом
- P-B1: гейт не взят на первом шаге (при этом стадия продолжается по лестнице).
- P-B2: на D = 10¹² ширина 10⁻⁸ и больше.
- P-B3: методы разошлись хоть раз ⇒ немедленная остановка и разбор, результат
  стадии не публикуется до устранения.
- P-B4: U₂ ≥ L, либо зазор вне (0.0190, 0.0210).
- P-B5: потеря больше 10⁻⁸ при D = 10¹².

## 5. Граница доказанности — записать честно

По итогам стадии в отчёте, README и письме формулировка должна разграничивать ровно так:

- **доказано**: Δ_max(11) лежит в предъявленном рациональном интервале; второе место
  топа строго ниже него;
- **не доказано**: что ни один из остальных ≈10⁹ графов не выше — это опирается на
  численные значения полного перебора и на звуковость отсева;
- **отсев доказуем отдельно**: χ(Ḡ) = α влечёт Δ = 0 через сэндвич α ≤ ϑ ≤ χ(Ḡ)
  (`SOURCES.md` S1.2), и это снимает вопрос примерно с 90 % пространства строго.

Округления в свою пользу не допускается ни в одной из трёх формулировок.

## 6. Обрыв ряда замкнутых форм

Отдельным абзацем в отчёте: ряд обрывается на n = 11 не от недостатка усилий, а потому
что число Ловаса для графов большего размера типично иррационально высокой степени;
красивые формы при n ≤ 10 — свойство малых размеров, а не правило. Подаётся как
результат с границей («не является корнем целого многочлена степени ≤ 48 при высоте
≤ …»), а не как неудача. **Литчек этого утверждения — после его формулировки, не до.**

## 7. Чего не делается

Не углублять PSLQ. Не считать n = 12. Не строить вилки для всего топ-1000: лидера и
второго достаточно для утверждения о максимуме. Не трогать закрытые Stage 6 и Stage 7.
