# REPORT — QUADC5, Stage 0

Дата: **2026-08-19**. Предрегистрация запечатана до первого сэмпла коммитом `dc07d1f`
(`PREREGISTRATION.sha256`: `e859786…` для `PREREGISTRATION.md`, `3a4fb60…` для
`SOURCES.md`). Воспроизведение целиком: `bash runners/run_stage0.sh`.

---

## РЕЗУЛЬТАТ

Перебор всех **261 080** связных неизоморфных графов на 9 вершинах закончен. Максимум
зазора достигается на единственном графе `HCRbdO{` (15 рёбер, α = 3), и его значение
**доказано точно**: ϑ = 11/3, **Δ = 2/3**. Не «численно опознано» — построены и
проверены в рациональной арифметике оба сертификата: примальный (целочисленная матрица
M, X = M/99 допустима и PSD, 1ᵀX1 = 11/3) и двойственный (рациональная B со
структурой Ловаса, λ_max(B) = 11/3 с собственными значениями {11/3⁴, 11/54², −11/6², −65/27}).
Для восьмивершинного оптимума авторы замкнутой формы не получили и назвали свою попытку
PSLQ ложноположительной (S1.9); здесь она есть и она точная.

Строение максимизатора описывается в одну строку: **шестиугольник C₆ плюс треугольник
K₃, каждая вершина треугольника соединена со своей антиподальной парой шестиугольника.**
Три вершины степени 4 — это и есть треугольник, шесть вершин степени 3 — шестиугольник.
Спектр лежит в ℚ(√2) — не в ℚ(√5), которым авторы объясняли восьмивершинный оптимум.

**Ответ на предрегистрированный вопрос — «частично, и не в той форме».** Три уровня были
зафиксированы до счёта: D1 (есть индуцированный C₅), D2 (два пятиугольника делят ребро),
D3 (буквальный аналог n=8: каждое ребро ровно в двух пятиугольниках, покрыты все рёбра).
Получено: **D1 — 50/50, D2 — 50/50, D3 — 0/50**, ровно как ожидалось в §6.2. Максимизатор
не беднее пятиугольниками, а богаче: 12 индуцированных C₅ против четырёх у Quad-C₅, и
все 15 рёбер покрыты — но с кратностями 2, 4 и 5, а не одинаковой двойкой. Идеальное
двукратное покрытие не воспроизводится ни у одного из топ-50.

Главное же в другом. Сама конструкция Quad-C₅ на девяти вершинах **выживает, но
перестаёт быть оптимумом**: ранги с 4-го по 31-й — 28 графов — стоят ровно на старом
значении Δ = 0.46784377 (в пределах 1e-7), и 26 из них содержат Quad-C₅ индуцированным
подграфом: это восьмивершинный оптимум плюс инертная девятая вершина. Строго обходят
плато ровно **три** графа, и ни один из них Quad-C₅ не содержит. То есть на 9 вершинах выигрывает не продолжение конструкции, а другая.
Меняется и размерность: Quad-C₅ был кутритным свидетелем (d\* = 3), у нового максимизатора
η₃ = 3.000000000000 = α **точно**, то есть в трёх измерениях преимущества нет вовсе, а
η₄ = 3.666666666667 = ϑ, то есть полное значение Ловаса достигается сразу в четырёх;
d\* = 4. У 49 из 50 графов топа d\* = 4.

Последовательность максимизаторов, теперь на четырёх размерах, пятиугольной
закономерности не образует: C₅ (один пятиугольник) → C₇ (**ни одного**) → Quad-C₅
(четыре, равномерно) → C₆+K₃ (двенадцать, неравномерно). Это было записано как довод
против закономерности до счёта (§6.2) и подтвердилось.

---

## Таблица предсказаний

### Слепые предсказания (записаны до счёта, §5 и §6.2 предрегистрации)

| # | предсказано | получено | допуск | вердикт |
|---|---|---|---|---|
| C1 | max Δ(9) > 0.46784373 | **0.6666666667** | — | **HIT** |
| C2 | C₉ не максимизатор, Δ(C₉) = 0.3600896 | Δ(C₉)=0.3600895896, ранг **370** из 261 080 | — | **HIT** |
| C3 | α максимизатора = 3 или 4 | 3 (и 3 у всех 50) | — | **HIT** |
| C4 | \|E\| максимизатора 10–14 | **15** | — | **MISS** |
| C5 | графов с Δ > 0.46784373 хотя бы один | 3 строго выше плато | — | **HIT** |
| D1 | максимизатор содержит индуцированный C₅ | да, 12 штук; 50/50 в топе | — | **HIT** |
| D2 | ≥2 пятиугольника делят ребро | да; 50/50 | — | **HIT** |
| D3 | идеального двукратного покрытия **не** будет | нет ни у одного; 0/50 | — | **HIT** |
| B-sound | отсев не выбросит ни одного графа с Δ>0 | 0 нарушений на n=8 и n=9 | τ | **HIT** |
| B-complete | ожидались нарушения (несовершенные с Δ=0) | 2814 на n=8, 117 770 на n=9 | — | **HIT** |

**C4 — единственный промах, и он содержательный.** Предсказание 10–14 рёбер опиралось на
то, что восьмивершинный оптимум разрежен (10 рёбер) и что в топ-10 на n=8 плотные графы
появляются только при α=2. Получилось 15. Механизм промаха виден: выигрышная конструкция
на 9 вершинах — не разреженный каркас из пятиугольников, а C₆ с наложенным треугольником,
и треугольник стоит трёх лишних рёбер. Наблюдение «оптимум разреженнее» (S1.8) на топ-50
держится лишь как слабая тенденция: ранговая корреляция Спирмена Δ против \|E\| равна
**−0.285 (p = 0.045)** при разбросе \|E\| от 13 до 18. Это на грани значимости и в
одиночном сравнении Quad-C₅ против Вагнера выглядит гораздо убедительнее, чем в списке.

### Калибровка 0.a — все ворота (значения из `results/report_0a.json`)

| # | предсказано [источник] | получено | допуск | вердикт |
|---|---|---|---|---|
| A1′ | 5cos(π/5)/(1+cos(π/5)) = √5 | 0.000e+00 | 1e-12 | PASS |
| A1″ | α(C₅) = 2 | 2 | точно | PASS |
| A1 | ϑ(C₅) = 2.236067977500 | 2.236067979342 | 1e-7 | PASS |
| A1‴ | **Δ(C₅) = 0.236067977500** | 0.236067979342 | **1e-6 ГЕЙТ** | **PASS** |
| A2 | связных графов n=7: 853 | 853 | точно | PASS |
| A2′ | argmax Δ при n=7 — C₇ | `FCp\`_` ≅ C₇ | изоморфизм | PASS |
| A2″ | ϑ(C₇) = 3.3176672074 | 3.3176672104 | 1e-7 | PASS |
| A2‴ | **Δ(C₇) против 0.31767 статьи** | 0.31766721 | **5e-6 ГЕЙТ** | **PASS** |
| A2‴b | **Δ(C₇) против аналитики** | 0.3176672104 | **1e-6 ГЕЙТ** | **PASS** |
| A3 | связных графов n=8: 11 117 | 11 117 | точно | PASS |
| A3′ | **argmax n=8 — `` GCQb`o ``** | `` GCQb`o `` | **строка ГЕЙТ** | **PASS** |
| A3″ | **max Δ = 0.46784373 (CSV авторов)** | 0.46784377 | **1e-6 ГЕЙТ** | **PASS** |
| A3″b | max Δ против 0.46784 статьи | 0.46784377 | 5e-6 | PASS |
| A3‴ | **ранг 2 = `` GCR`r_ ``, ранг 3 = `` GCrb`o ``** | совпали | **строка ГЕЙТ** | **PASS** |
| A3‴b | Δ(ранг 2) = 0.43844718 | 0.43844718 | 1e-6 | PASS |
| A3⁗ | ранг 3 ≅ граф Вагнера | изоморфен | изоморфизм | PASS |
| A3⁗ | **Δ(Вагнер) = 0.41421356** | 0.41421364 | **1e-6 ГЕЙТ** | **PASS** |
| obs-8 | ϑ(Вагнер) = 2+√2 (**наше**, не из источника) | 3.4142136393 | 1e-7 | PASS |
| A4 | ранги 4–6 как множество | совпало | равенство множеств | PASS |
| — | SDP-калибровка, макс. ошибка SCSdirect | 8.887e-09 | 1e-7 | PASS |
| — | SDP-калибровка, макс. ошибка CLARABEL | 1.397e-08 | 1e-7 | PASS |
| — | массовый солвер против точного, топ-50 n=8 | 8.20e-08 | 1e-5 | PASS |

**ГЕЙТ 0.a ПРОЙДЕН.** Все три опорные точки воспроизведены своим конвейером.

Проверка «может ли этот тест провалиться» отработала буквально: на первом прогоне
A2′ и A3⁗ **провалились**. Причина оказалась в тесте, а не в числах — graph6 кодирует
помеченный граф, и канонический код Маккея сравнивался с нашей собственной нумерацией
вершин. Числа при этом сошлись все. Тест исправлен на сравнение по изоморфизму
(`quadc5/g6.py::iso_equal`); предсказания не менялись. Это и есть ошибка №4 из списка
«какая именно ошибка провалила бы этот тест» (§3.1) — только поймала она не конвейер,
а сам тест.

### Правило репозитория (гейт R, `tests/test_estimators.py`)

500 случайных связных графов, n ∈ {5…9}, сид 20260819. Все семь проверок пройдены:
α двумя путями (0 расхождений), сэндвич α ≤ ϑ ≤ χ(Ḡ) (0 нарушений), два солвера
(макс. 8.92e-08), третий солвер (4.28e-07), первичная невязка (1.26e-08),
инвариантность к перенумерации (0 нарушений), ϑ пустого и полного графа (0 нарушений).

---

## Сверка с опубликованным кодом

Опубликованный `certification.py` (репозиторий авторов, коммит `bfacfd0`; тот же код
в архиве Zenodo 20465134, побайтовое совпадение по всем шести содержательным файлам)
прогнан на этой машине в отдельном venv. Скрипт отработал до конца, код возврата 0.
Выжимка вывода — `results/authors_run_summary.txt`.

Ниже перечислено только то, что наблюдалось: числа с обеих сторон и условия прогона.
Причины не устанавливаются, оценки не даются.

**Совпало.** Максимизатор `` GCQb`o ``; ранги 1–3 и их коды; ϑ = 3.46784373;
α по всем 11 117 связным графам; η₃(Quad-C₅) = 1 + √5 (расхождение 3.4·10⁻¹³);
значения ϑ от CLARABEL на 44 графах, общих для двух версий топ-50, — побитово,
разница 0.000·10⁰. **Ведущие записи не затронуты ни одним из наблюдений ниже.**

**Наблюдения.**

1. Таблица топ-10 статьи (S1.5) приписывает рангу 4 одиннадцать рёбер, рангу 5 —
   двенадцать. В `all_n8_results.csv` из репозитория порядок обратный (S2.4). Наш
   прогон опубликованного кода дал порядок, совпадающий с таблицей статьи
   (ранг 4 = `GCQbdo`, 11 рёбер). Значения Δ у трёх графов этой группы различаются на
   2·10⁻⁸. Наш независимый конвейер дал третий порядок в той же группе.
2. Между `top50_certification.csv` из репозитория и результатом нашего прогона того же
   кода общими оказались 44 графа из 50; шесть замыкающих различаются. Массовые
   значения Δ от SCS разошлись максимум на 2.0·10⁻⁸ при среднем 4.2·10⁻¹⁰.
3. `eta3_certification.csv` из репозитория содержит `interval_lo = 3.2360561719` и
   `n_feasible_restarts = 500`. Наш прогон того же кода с тем же сидом
   (`np.random.seed(42)`, строка 393) дал `interval_lo = 3.0035598350` и
   `n_feasible_restarts = 499`. Наилучшее значение η₃ совпало с 1 + √5 в обоих случаях.
4. Число рестартов в тексте статьи указано как 300 в пяти местах (`paper.tex` 369, 379,
   568, 591, 1278) и как 500 в двух (624, 1137). В коде `N_RESTARTS = 500`.
5. README репозитория указывает CSV-файлы по путям вида `data/…`; в репозитории они
   лежат в корне, а скрипт создаёт `data/` и пишет туда (`os.makedirs`, строка 256).

**Что из этого следует для нашей работы.** Опорные значения гейта 0.a брались из
8-значного CSV, а не из 5-значной таблицы статьи, и допуск 10⁻⁶ применялся только к ним
(предрегистрация §2): к напечатанным пяти знакам такой допуск неприменим арифметически.
Ранги ниже третьего в гейт не включались, потому что различия внутри их группы лежат
на уровне 2·10⁻⁸. Оба решения записаны до счёта.

---

## 0.b — отсев до SDP

Реализованы два отсева. **F1** — предписанный брифом: распознавание совершенных графов
через индуцированные нечётные дыры и антидыры длины ≥5 (для n ≤ 9 это C₅, C₇, C₉ в G и
в Ḡ). **F2** — за пределами брифа: χ(Ḡ) = α, что даёт Δ = 0 напрямую из процитированного
сэндвича α ≤ ϑ ≤ χ(Ḡ) (S1.2), без обращения к сильной теореме о совершенных графах,
которой ни один наш источник не формулирует.

| | n = 8 | n = 9 |
|---|---|---|
| связных графов | 11 117 | 261 080 |
| с Δ = 0 | 10 619 (95.52 %) | 244 547 (93.67 %) |
| **с Δ > 0** | **498** | **16 533** |
| F1 снимает | 7 805 (70.21 %) | 126 777 (48.56 %) |
| F1 ложных срабатываний | **0** | **0** |
| F1 пропусков | 2 814 | 117 770 |
| F2 снимает | 10 619 (95.52 %) | 244 541 (93.67 %) |
| F2 ложных срабатываний | **0** | **0** |
| F2 пропусков | **0** | **6** |
| из пропусков F1 ловится F2 | 2 814 (все) | 117 764 из 117 770 |

**Гейт B-sound пройден на обоих размерах: ни один отсев не выбросил ни одного графа с
Δ > 0.** Литеральный гейт брифа («выбросить ровно те, у которых Δ < 1e-9, и ни одного
лишнего») **провален отсевом F1** — ровно как записано в ожидании §4.2. Причина
установлена и не гипотетична: все 2814 пропущенных на n=8 графов имеют χ(Ḡ) = α, то
есть сэндвич схлопывается и без совершенства. На n=9 F1 пропускает 45 % всех нулей —
на девяти вершинах несовершенных графов с ϑ = α становится больше, чем совершенных.

F2 промахивается всего на 6 графах из 261 080 (`H?q`qjo`, `HCOf@pS`, `HCQf@pS`,
`HEhethk`, `HEhethm`, `HEhuTxm`) — у них ϑ = α, но χ(Ĝ) = α + 1, то есть сэндвич не
схлопывается, а равенство всё равно есть. Полным критерием нуля ни один из двух не
является.

**О допуске 1e-9 из брифа.** Он лежит ниже шума солверов (собственная невязка CLARABEL
на этих задачах доходит до 1.1e-07). Применённый буквально, он объявил бы 5441 истинный
нуль ненулевым. Но Δ резко двумодально: верх нулевого кластера 1.06e-07 (n=8) и
8.96e-07 (n=9), а наименьший строго положительный зазор — 0.025753 (n=8) и 0.003657
(n=9). Классификация одинакова при **любом** пороге внутри этого разрыва шириной в
3–5 порядков, поэтому она от порога не зависит и взята на 1e-6.

**Сколько времени экономит отсев.** F2 оставляет под SDP 6.33 % графов на n=9, то есть
сокращает счёт примерно в 16 раз при стоимости самого отсева 0.02 мс против 5 мс на SDP.
Тем не менее **в боевом прогоне отсев не применялся**: SDP посчитан по всем 261 080
графам (страховка §4.2), что и позволило проверить гейт не только на n=8, но и на n=9.
Полный прогон занял 382 с на 7 процессах — цена страховки оказалась ничтожной.

---

## 0.c — перебор n=9

261 080 связных неизоморфных графов (число получено собственной выгрузкой базы Маккея,
S3, а не принято из брифа). Сид 20260819, детерминированный перебор, чанки по 500 с
непрерывной записью в `results/n9_partial_*.csv`, прогон возобновляем. Массовый солвер —
SCS, собранный напрямую в коническую форму без канонизации cvxpy, eps = 1e-8, 5 мс на
граф. Время: **382 с на 7 процессах**. GPU не использовался: он не нужен — каждая
задача 9×9, и на CPU весь перебор укладывается в шесть минут.

Контроль точности: топ-**1000** (бриф просил топ-100; взято больше, чтобы сама граница
сотни не была артефактом ранжирования) пересчитан CLARABEL. Максимальное расхождение
|ϑ_SCS − ϑ_CLARABEL| = **9.67e-08**, ни один граф не превысил τ_hi = 1e-7. Топ-100
дополнительно посчитан третьим солвером (CVXOPT): максимальное отклонение от CLARABEL
3.05e-07, и 48 графов из 100 вышли за 1e-7. Это **собственная точность CVXOPT**, а не
расхождение по существу: на аналитическом семействе, где ответ известен, CVXOPT ошибается
на 1.42e-07 (C₅) и 1.30e-07 (C₉). Значения не усреднялись — расхождения перечислены
поимённо в `results/third_solver_n9_top100.json`, как и предписано §5.

### Топ-20 по Δ (полные 50 — в `results/n9_top1000.csv`)

| ранг | graph6 | \|E\| | α | ϑ | Δ | #C₅ | покрыто | d\* |
|---:|---|---:|---:|---|---|---:|---:|---:|
| 1 | `HCRbdO{` | 15 | 3 | **11/3 точно** | **2/3 точно** | 12 | 15/15 | 4† |
| 2 | `` HCrb`qi `` | 16 | 3 | 3.6180340009 | 0.6180340009 | 12 | 16/16 | 4† |
| 3 | `` HCQe`pc `` | 13 | 3 | 3.4703808534 | 0.4703808534 | 4 | 13/13 | 3 |
| 4 | `HCQv@ps` | 15 | 3 | 3.4678437744 | 0.4678437744 | 7 | 13/15 | 4† |
| 5 | `` HCQb`r} `` | 17 | 3 | 3.4678437725 | 0.4678437725 | 4 | 10/17 | 4† |
| 6 | `` HCQb`r~ `` | 18 | 3 | 3.4678437704 | 0.4678437704 | 4 | 10/18 | 4† |
| 7 | `` HCQb`rK `` | 14 | 3 | 3.4678437676 | 0.4678437676 | 8 | 14/14 | 4† |
| 8 | `` HCQb`py `` | 15 | 3 | 3.4678437646 | 0.4678437646 | 5 | 12/15 | 4† |
| 9 | `HCQbR_y` | 14 | 3 | 3.4678437622 | 0.4678437622 | 5 | 12/14 | 4† |
| 10 | `` HCQb`rY `` | 15 | 3 | 3.4678437590 | 0.4678437590 | 6 | 13/15 | 4† |
| 11–20 | … | 13–18 | 3 | | 0.46784375…0.46784374 | 4–8 | | 4† |

† — d\*, опирающийся на ненайденное превышение η₃ > α, а не на доказательство; та же
оговорка, что у авторов (S1.9).

Значение ϑ ранга 2 численно равно (5+√5)/2 = 3.6180339887 (расхождение 1.2e-08). Это
**наше наблюдение, а не результат из источника**, и точный сертификат для него не
строился — помечается как численно указанное.

Ниже ранга 3 идёт плато: ранги 4–31, то есть 28 графов, имеют Δ = 0.46784377 в
пределах 1e-7 — ровно восьмивершинное значение. Ранг 32 падает до 0.4674943.

---

## 0.d — структура и ответ на предрегистрированный вопрос

**Уровни, зафиксированные до счёта (§6.2), и результат:**

| уровень | формулировка | ожидание | получено (максимизатор / топ-50) | вердикт |
|---|---|---|---|---|
| D1 | есть индуцированный C₅ | да | 12 штук / **50 из 50** | HIT |
| D2 | ≥2 пятиугольника делят ребро | да | да / **50 из 50** | HIT |
| D3 | каждое ребро ровно в двух, покрыты все | **нет** | нет / **0 из 50** | HIT |

Арифметический довод, записанный до счёта, подтвердился: D3 требует 5·(#C₅) = 2·\|E\|,
то есть \|E\| кратно 5. У максимизатора 15 рёбер — кратность есть! — но #C₅ = 12, а не
6, и кратности рёбер распределены как {2: 3 ребра, 4: 6 рёбер, 5: 6 рёбер}. Делимость
оказалась не препятствием; препятствием оказалось то, что пятиугольников слишком много.

**Прочие величины по топ-50** (`results/report_0d_n9.json`):
- α = 3 у всех пятидесяти без исключения;
- \|E\| от 13 до 18; ранговая корреляция Спирмена Δ против \|E\| = **−0.285 (p = 0.045)**;
- число индуцированных C₅ от 4 до 12; все рёбра покрыты пятиугольниками у 15 из 50;
- d\* = 4 у 49 из 50, d\* = 3 только у ранга 3 (`` HCQe`pc ``);
- степенные последовательности — в JSON; у максимизатора (3,3,3,3,3,3,4,4,4).

**Развёрнутый ответ.** Если понимать вопрос буквально — «повторится ли конструкция
Quad-C₅» — ответ **нет**: идеального двукратного покрытия нет ни у одного графа топ-50,
а сама Quad-C₅, дополненная девятой вершиной, даёт лишь плато на старом значении
0.46784377 и проигрывает 0.6667. Ни один из трёх графов, строго обошедших плато, Quad-C₅
индуцированным подграфом не содержит (проверено перебором всех 8-вершинных
индуцированных подграфов: содержат её 26 графов топ-50, все — с 5-го ранга и ниже;
ранг 4 стоит на плато, но Quad-C₅ тоже не содержит).

Если понимать вопрос как «остаются ли перекрывающиеся пятиугольники механизмом» —
ответ **да, и сильнее прежнего**: максимизатор состоит из 12 индуцированных C₅,
покрывающих все его рёбра. Но это уже другая геометрия — не четыре пятиугольника,
попарно делящие по ребру, а плотная система, где ребро может лежать в пяти
пятиугольниках сразу.

Так что основания для структурного критерия по трём размерам (5, 8, 9), о котором
говорил бриф, **нет**: во-первых, ряд рвётся на n=7, где максимизатор C₇ не содержит
ни одного пятиугольника; во-вторых, на n=9 равномерность покрытия исчезает; в-третьих,
меняется размерность — восьмивершинный оптимум был кутритным (d\* = 3), девятивершинный
в трёх измерениях не даёт **ничего** (η₃ = α точно) и требует четырёх.

---

## Точный сертификат для ϑ = 11/3 (`runners/certify_n9_max.py`)

Примал. Целочисленная симметричная M со следом 99, нулями на всех 15 рёбрах и
sum(M) = 363; X = M/99 допустима для SDP из S1.2, и 1ᵀX1 = 363/99 = 11/3. PSD проверена
точно: все 511 главных миноров ≥ 0 в рациональной арифметике; собственные значения M
целые — 0 (кратность 5), 17 (2), 24, 41, то есть ранг 4. Отсюда **ϑ ≥ 11/3**.

Двойственное. Симметричная рациональная B, равная 1 на диагонали и на всех нерёбрах,
получена решением условия дополняющей нежёсткости (11/3·I − B)·M = 0 — система имеет
единственное решение. На рёбрах B принимает три значения: −5/6 (6 рёбер), −2/9 (6 рёбер),
−28/27 (3 ребра). Собственные значения B точно равны {11/3 (×4), 11/54 (×2), −11/6 (×2),
−65/27}, и (11/3·I − B) PSD в точной арифметике. Отсюда **ϑ ≤ 11/3**.

Итого ϑ(`HCRbdO{`) = 11/3, α = 3, **Δ = 2/3** — доказано, не подогнано.

---

## Источники

Все обращения **2026-08-19**, выгрузки в `sources/`, дословные цитаты с номерами строк
в `SOURCES.md`.

| источник | ссылка | что взято |
|---|---|---|
| arXiv:2605.12828 v2 (Tamer, Müstecaplıoğlu, Dizdar, Gedik) | https://arxiv.org/abs/2605.12828 | определение Δ, SDP, определение d\*, опорные значения n=5/7/8, код и рёбра Quad-C₅, структура пятиугольников |
| репозиторий авторов, коммит `bfacfd0` | https://github.com/ugurtamerphys/quad-c5-contextuality | `certification.py`, 8-значные Δ для всех 11 117 графов, топ-50, η₃ |
| Zenodo 20465134 | https://doi.org/10.5281/zenodo.20465134 | архив кода; побайтово идентичен GitHub |
| база графов Маккея | https://users.cecs.anu.edu.au/~bdm/data/ | `graph7c.g6` (853), `graph8c.g6` (11 117), `graph9c.g6` (261 080) |

Версии: v1 подана 12 мая 2026, v2 — 31 мая 2026; выгружена v2. Датировка на первой
странице PDF — «June 2, 2026», на два дня позже подачи v2; расхождение отмечено, ни на
что не влияет.

**UNVERIFIED.** Значение ϑ графов Кнезера ни в одном источнике проекта не процитировано.
Оно исключено из калибровки SDP и не участвует ни в одном сравнении (S4).

---

## Отклонения от брифа

1. **Графы Кнезера исключены из калибровки SDP.** Бриф называл их среди аналитических
   семейств, но формулы для ϑ(K(n,k)) в источниках проекта нет; по правилу 0 она
   помечена UNVERIFIED. Калибровка опирается на циклы, полные и пустые графы, где
   значения выведены из процитированного сэндвича. Частный случай C₅ = K(5,2) покрыт
   формулой циклов.
2. **Порог «Δ = 0» взят 1e-6, а не 1e-9.** Причина в §0.b: 1e-9 лежит ниже шума
   солверов, и буквальное применение сломало бы классификацию. Оба варианта посчитаны и
   напечатаны; разрыв между кластерами в 3–5 порядков делает выбор порога безразличным.
3. **Отсев не применялся к боевому прогону.** SDP посчитан по всем 261 080 графам.
   Это дороже (382 с вместо примерно 30 с), но позволило проверить гейт отсева на n=9,
   а не только на n=8, как просил бриф.
4. **Добавлен второй отсев F2** (χ(Ḡ) = α), которого в брифе нет. Он строго сильнее F1,
   и его звуковость выводится из процитированного сэндвича, а не из сильной теоремы о
   совершенных графах, которой в источниках нет.
5. **Проверено топ-1000, а не топ-100.** Вторым солвером пересчитано в десять раз больше,
   чем просил бриф, чтобы граница сотни сама не оказалась артефактом. Третий солвер —
   на топ-100.
6. **GPU не использовался.** Разрешение было, необходимости нет: весь перебор — шесть
   минут на 7 ядрах CPU, каждая SDP 9×9.
7. **Тест A2′/A3⁗ исправлен после первого прогона** — сравнение кодов graph6 заменено на
   сравнение по изоморфизму. Предсказания не менялись, числа не менялись; менялся способ
   проверки. Зафиксировано выше в разделе 0.a.
8. **Код `GCQb'o` из брифа заменён на `` GCQb`o ``** — опечатка транслитерации,
   опровергнутая выгрузкой (S1.6).
9. **Построен точный сертификат для ϑ на n=9**, чего бриф не требовал. Сделано потому,
   что численное значение подозрительно точно совпало с 11/3, а источник прямо
   предупреждает о ложноположительных опознаниях такого рода (S1.9).
