# PREDICTION — верхние слои при n = 12, записано до всякого счёта на двенадцати вершинах

Дата: **2026-08-24**. Запечатывается в `PREDICTION_N12_LAYERS.sha256`, коммит печати
предшествует любому вычислению на двенадцати вершинах. На момент записи ни один
двенадцативершинный граф не считался ни в этой кампании, ни в какой другой её части.

Полный перебор n = 12 **не планируется**: связных графов там порядка 10¹¹ против
1 006 700 565 на одиннадцати (множитель 85.9 за шаг, обе величины получены здесь из
`geng -u`). Предсказание записывается затем, чтобы, если кто-нибудь когда-нибудь посчитает
двенадцать вершин, проверка была готова заранее и её дата была старше данных.

## Основание

Stage 7 установил (`REPORT_STAGE7.md`): конус C_k(G) = {v} ∨ (G ⊔ K̄_{k−1}) переносит Δ
**ровно**, сдвигая α и ϑ на одно и то же целое k − 1. Максимизатор на m вершинах с
α = a_m попадает при этом на n вершин в слой

    a = a_m + (n − m) − 1

со значением Δ_max(m). Верхние слои при n = 9, 10 не порождают собственных значений:
граница наследования a\* = 5 на обоих размерах. Отсюда предсказание для n = 12 —
арифметика по уже известному ряду, без единого SDP.

Использованные значения ряда, все установленные ранее и с сертификатами:

| m | α максимизатора | Δ_max(m) |
|--:|--:|---|
| 5 | 2 | √5 − 2 |
| 7 | 3 | корень x³+7x²−49x+49 в (3,4), минус 3 |
| 8 | 3 | корень x⁴−x³+23x²−155x+158 в (3,4), минус 3 |
| 9 | 3 | 2/3 |
| 10 | 3 | 1/√2 |
| 11 | 4 | в [0.7748885327027013, 0.7748885327466875] |

## ПРЕДСКАЗАНИЕ

**P-12-L. Граница наследования при n = 12 равна a\* = 5**, и для всех слоёв a ≥ 5
значение равно границе переноса **точно**:

| слой a | D(12, a) | точная форма | десятично | перенос из |
|--:|---|---|---|---|
| **5** | Δ_max(9) | **2/3** | 0.66666666666666667 | n = 9, слой 3 |
| **6** | Δ_max(8) | корень квартики − 3 | 0.46784372984022411 | n = 8, слой 3 |
| **7** | Δ_max(7) | корень кубики − 3 | 0.31766720739409539 | n = 7, слой 3 |
| **8** | Δ_max(5) | **√5 − 2** | 0.23606797749978970 | n = 5, слой 2 |

Слои a ≥ 9 при n = 12 предсказываются **пустыми** (Δ = 0): переносить в них нечего.

**P-12-B. Слои a ≤ 4 порождают собственные значения**, строго превышающие перенос.
Границы переноса для них: T(12,4) = Δ_max(11) ≈ 0.7748885327, T(12,3) = D(11,3) ≈
0.7355048965, T(12,2) = D(11,2) ≈ 0.5027755676. Предсказывается D(12,a) > T(12,a) при
a = 2, 3, 4.

**P-12-M. Δ_max(12) достигается в слое a = 4 или a = 5**, то есть максимизатор имеет
α ∈ {4, 5}. Основание: слой 4 выиграл при n = 11, а приращения слоя 4 росли (0.1240,
0.1907, 0.2241), тогда как слой 3 затухал (0.1988, 0.0404, 0.0284). Это самая слабая
из трёх записей и записана как таковая.

## Что считается провалом

- P-12-L: любой слой a ≥ 5 при n = 12 даёт значение, отличное от указанного больше чем
  на 10⁻⁶; либо граница наследования не равна 5.
- P-12-B: любой из слоёв 2, 3, 4 оказался равен своей границе переноса.
- P-12-M: α максимизатора вне {4, 5}.

## Оговорки, записанные вместе с предсказанием

1. **Нижняя часть P-12-L доказана, верхняя — нет.** Что D(12,a) ≥ указанного значения,
   следует из конуса точно. Что в слое нет ничего большего — предсказание, и именно оно
   может провалиться: при n = 11 слой 4 порождал своё, и ничто не запрещает слою 5 начать
   порождать своё при n = 12.
2. **Граница a\* могла сдвигаться и раньше**, но не сдвигалась: 5 при n = 9, 5 при n = 10,
   5 при n = 11 (последнее — вне вердикта Stage 7, поскольку гипотеза выросла из n = 11).
   Три точки — слабое основание для четвёртой, и это признаётся здесь, а не потом.
3. **P-12-M — экстраполяция того же рода, что провалилась на n = 11.** Метод послойной
   экстраполяции дал там одно попадание из десяти, и это попадание было совпадением.
   Записывается как явно названный риск.
