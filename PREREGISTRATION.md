# PREREGISTRATION — QUADC5, Stage 0

Написано **до первого сэмпла**. После того как хэш из `PREREGISTRATION.sha256`
закоммичен, этот файл не редактируется; всё, что придётся исправить, идёт в `REPORT.md`
как датированная поправка, называющая, что именно она меняет.

Репозиторий: `/home/artem/QUADC5`, отдельный git-репозиторий, свой `.venv`.
Из прошлых кампаний (TOMLOC, DSNET, WORLDLINE) не переиспользуется ничего: ни строки
кода, ни числа. Стоящие правила восстановлены по формулировкам, а не по памяти о
результатах.

Дата написания: **2026-08-19** (UTC).
Прочитано на момент написания: весь `SOURCES.md` (S1–S4), выгрузки в `sources/`
(статья arXiv:2605.12828 v2 — PDF, LaTeX-исходник, abs-страница; репозиторий авторов на
коммите `bfacfd0`; архив Zenodo 20465134; три файла базы Маккея).
**Ни одного вычисления к этому моменту не запущено.** Единственное, что делалось с
данными — `wc -l` по выгрузкам Маккея и чтение CSV авторов; и то и другое есть чтение
источника, а не работа конвейера, и зафиксировано в `SOURCES.md` S2.4 и S3.

Окружение: Python 3.12.3, `.venv` с numpy 2.5.2, scipy 1.18.0, networkx 3.6.1,
cvxpy 1.9.2, cvxopt 1.3.3, scs 3.2.11, clarabel. Солверы, видимые cvxpy:
CLARABEL, SCS, CVXOPT (плюс не-SDP: GLPK, SCIPY, HIGHS, OSQP). 8 ядер CPU,
GPU — RTX 4070 Ti 12 ГБ под WSL2.

---

## 0. Стоящие правила в применении к QUADC5

- **Правило 0 (усиленная редакция).** Ни одно несущее число и ни одно утверждение о
  содержании чужой работы не берётся по памяти — ни моей, ни из брифа. Каждое либо
  выводится в этом репозитории, либо подтверждается прямой выгрузкой источника в
  `sources/` со ссылкой и датой обращения в `SOURCES.md`. Недостижимый источник ⇒
  UNVERIFIED и вне всех решающих сравнений. **Это относится и к пересказам источников в
  самом брифе:** код `GCQb'o` из брифа уже опровергнут выгрузкой (S1.6, правильный —
  `` GCQb`o ``), а число 261 080 уже подтверждено выгрузкой (S3), а не принято на слово.
- **Правило калибровки.** 0.a — ворота. Если падает любой из трёх пунктов (n=5, n=7,
  n=8), 0.c не запускается и стадия отчитывается как заблокированная на 0.a.
- **Правило репозитория.** Каждый оценщик гоняется на случайных входах, а не только на
  «красивых» графах, где ответ известен. Здесь: α и ϑ проверяются на случайных
  G(n,p) с случайным p и случайным n ∈ [5,9], каждый — двумя независимыми путями
  (§R ниже). Красивые случаи (циклы, полные, пустые, Вагнер) необходимы, но не
  достаточны: на них не ловится, например, ошибка индексации вершин, потому что все они
  вершинно-транзитивны.
- **Правило kill.** Сработавший kill пишется как отрицательный результат с
  диагностикой. Ни одна новая величина не вводится после того, как данные увидены.
- **Тест, не способный провалиться, вакуумен.** Для каждых ворот ниже назван **конкретный
  баг**, который эти ворота обязаны поймать, и это отчитывается вместе с воротами.
- **Предрегистрация.** Этот файл + `SOURCES.md`, SHA-256 в `PREREGISTRATION.sha256`,
  закоммичены до первого сэмпла.

### 0.0. Слепые и неслепые предсказания

Правило 0 заставляет читать источники до работы, поэтому часть ответов уже известна.
Разметка:
- **[ИЗ ИСТОЧНИКА]** — значение прочитано в `SOURCES.md`; предсказание проверяет мой
  конвейер, а не мою догадку. Все опорные значения 0.a таковы.
- **[СЛЕПОЕ]** — значение на момент написания неизвестно ниоткуда: результаты n=9 (0.c),
  все структурные величины (0.d), поведение отсева (0.b).

---

## 1. Что именно считается (определения, зафиксированные до кода)

Все три взяты из `SOURCES.md`, не из памяти.

- **Зазор** (S1.1): Δ(G) = ϑ(G) − α(G), обе величины — для одного и того же графа G,
  рёбра G суть отношения исключительности. Δ ≥ 0 по теореме о сэндвиче (S1.2).
- **ϑ** (S1.2): ϑ(G) = max { 1ᵀX1 : X ⪰ 0, Tr X = 1, X_ij = 0 ∀ (i,j) ∈ E }.
  Симметричная вещественная X размера n×n.
- **α**: число независимости, максимальный по мощности набор попарно несмежных вершин.
- **d\*** (S1.3): d\*(G) = min { d : η_d(G) > α(G) }, где η_d — максимум наибольшего
  собственного значения Σ|v_i⟩⟨v_i| по единичным векторам в ℝ^d, ортогональным вдоль
  рёбер. **Невыпуклая задача**; всё, что даёт многократный рестарт — нижняя оценка η_d.
  Поэтому: «η_d > α» устанавливается конструктивно и надёжно, а «η_d = α» — только как
  результат-нижняя-граница, с той же оговоркой, что у авторов (S1.9). Любое d\*, где
  вывод опирается на ненайденное превышение, помечается **†numerically indicated**.

Правило именования: везде используется каноническая форма graph6 из `nx.to_graph6_bytes`,
и она же проверяется на совпадение с кодами из S1.6/S2.4.

---

## 2. Допуски и их обоснования

Каждый допуск сопровождается причиной — этого требует правило.

| символ | значение | к чему | обоснование |
|---|---|---|---|
| τ_an | 1e-12 | аналитические тождества (не SDP) | бриф требует 1e-12 для n=5; на чистой арифметике double это ~4 порядка запаса над машинным epsilon 2.2e-16 |
| τ_hi | 1e-7 | ϑ от солвера высокой точности (CLARABEL) против аналитики | S1.9: авторы заявляют для CLARABEL точность 1e-10…1e-12; беру три порядка запаса, потому что их заявление о точности само не проверено мной |
| τ_bulk | 1e-5 | ϑ от массового солвера (SCS) против CLARABEL | S1.9: SCS даёт 1e-4…1e-6; беру верх этого диапазона |
| τ_gate | 1e-6 | гейт 0.a по Δ | число из брифа. Применимо **только** к 8-значным величинам из CSV авторов (S2.4), а не к 5-значным из статьи |
| τ_paper | 5e-6 | сверка с напечатанными в статье Δ | половина последнего печатного разряда: «0.46784» означает интервал [0.467835, 0.467845], сверять с ним точнее 5e-6 невозможно в принципе |
| τ_zero | 1e-9 | порог «Δ равен нулю» | число из брифа (0.b). Между τ_hi и τ_bulk по величине; поэтому решение «Δ=0» принимается только по CLARABEL-значению |

**Фиксация допуска SDP (требование брифа).** τ_hi и τ_bulk выше объявлены априори из
заявлений источника. Дополнительно в 0.a измеряется фактическая ошибка на аналитическом
семействе (§3.2) и в `REPORT.md` печатается max|ϑ_числ − ϑ_анал|. Если измеренный
максимум превысит объявленный τ_hi, это **не** повод переопределять τ_hi постфактум:
это отказ калибровки SDP, и он отчитывается как таковой.

**Почему сравнение с 5-значными числами статьи не может быть гейтом.** Если бы гейт
0.a применялся к «0.46784» с допуском 1e-6, он был бы невыполним при любом верном коде —
тест, который обязан провалиться, столь же бесполезен, как тест, который не может
провалиться. Гейт применяется к 0.46784373 из `all_n8_results.csv` (S2.4).

---

## 3. Блок 0.a — калибровка на чужом коде

### 3.1. Три обязательных пункта

| # | что | предсказано [источник] | допуск | что означает провал |
|---|---|---|---|---|
| A1 | n=5, ϑ(C₅) | √5 = 2.2360679774997896 [ИЗ ИСТОЧНИКА S1.4] | τ_hi | ошибка в SDP-формулировке |
| A1′ | n=5, тождество 5cos(π/5)/(1+cos(π/5)) = √5 | тождество [вывод] | τ_an | ошибка в формуле циклов |
| A1″ | n=5, α(C₅) | 2 [S1.4] | точно | ошибка в α |
| A1‴ | n=5, Δ(C₅) | √5 − 2 = 0.2360679774997896 | τ_hi | — |
| A2 | n=7, число связных графов | 853 [S1.4, S3] | точно | ошибка чтения g6 или фильтра связности |
| A2′ | n=7, argmax Δ | C₇ [S1.4] | — | ошибка ранжирования |
| A2″ | n=7, ϑ(C₇) | 7cos(π/7)/(1+cos(π/7)) = 3.3176672...  [S1.4] | τ_hi | — |
| A2‴ | n=7, Δ(C₇) против таблицы иерархии | 0.31767 [S1.4] | τ_paper | — |
| A3 | n=8, число связных графов | 11 117 [S1.12, S3] | точно | — |
| A3′ | n=8, argmax Δ, код graph6 | `` GCQb`o `` [S1.6, S2.4] | строгое равенство строк | ошибка канонизации graph6 |
| A3″ | n=8, max Δ | 0.46784373 [S2.4, CSV] | τ_gate | — |
| A3‴ | n=8, ранги 2 и 3 | `` GCR`r_ ``, `` GCrb`o `` [S2.4] | строгое равенство | — |
| A3⁗ | n=8, Δ(Вагнер) | 0.41421356 [S2.4]; он же ранг 3 [S1.5] | τ_gate | — |
| A4 | n=8, множество рангов 4–6 | { `` GCRb`w ``, `GCQbdo`, `` GCp`dO `` } как **множество** [S2.4] | τ_gate по Δ | — |

Пункт A4 намеренно сформулирован как равенство множеств, а не последовательностей:
`SOURCES.md` S2.4 показывает, что статья и данные самих авторов расходятся в порядке
рангов 4 и 5, а три значения Δ различаются на 2e-8, то есть внутри погрешности их
солвера. Требовать здесь порядок значило бы поставить гейт, который провалится по чужой
неоднозначности, а не по нашей ошибке.

**ГЕЙТ 0.a:** расхождение по Δ больше τ_gate = 1e-6 на любом из A1‴/A2‴/A3″ — стоп,
0.b и 0.c не запускаются, разбирательство пишется в `REPORT.md`.

**Какая именно ошибка провалила бы этот тест.** Перечислено до кода:
1. Перепутать G и Ḡ в SDP (обнулять недостающие рёбра вместо рёбер) — тогда на C₅ выйдет
   ϑ(C̄₅)=ϑ(C₅)=√5 и A1 пройдёт, но на n=7 и n=8 argmax окажется другим, и A2′/A3′ упадут.
   **Это главная причина, по которой одного C₅ для калибровки недостаточно.**
2. Взять ϑ по формуле «max Σ|⟨ψ|v_i⟩|²» с неединичными векторами — даст завышение,
   поймается на A1 (τ_hi).
3. Считать α как размер максимальной клики самого G вместо дополнения — на C₅ даст 2
   (совпадёт!), на C₇ даст 2 вместо 3, поймается на A2‴.
4. Ошибка порядка вершин при чтении graph6 (обратный порядок бит) — все циклы и полные
   графы переживут её, а A3′ упадёт: код победителя не совпадёт побитово.
5. Сравнить с 5-значным «0.46784» и «подтвердить» с допуском 1e-6 — невозможно;
   именно поэтому гейт привязан к CSV (см. §2).

### 3.2. Калибровка SDP на аналитическом семействе

Считается ϑ и сверяется с аналитикой:
- циклы C₅, C₇, C₉ по формуле n cos(π/n)/(1 + cos(π/n)) [S1.4 даёт её для n=7];
  предсказания: 2.2360679775, 3.3176672074, 4.3600895814 (пересчитываются кодом, здесь
  печатаются как ожидание);
- полные графы K₅…K₉: ϑ = 1 [вывод из сэндвича, S4];
- пустые графы на 5…9 вершинах: ϑ = n [вывод из сэндвича, S4];
- **графы Кнезера: исключены.** Значение ϑ(K(n,k)) в источниках проекта не процитировано,
  помечено UNVERIFIED (S4) и в калибровке не участвует. Отклонение от брифа, записано.

Отчитывается max|ϑ_числ − ϑ_анал| по всем трём семействам, по каждому солверу отдельно.

**Ошибка, которую это ловит:** неверный знак или масштаб в целевой функции (1ᵀX1 против
Tr X или против Σ_{i<j}) — пустые графы дают ϑ=n и мгновенно разоблачают масштаб, полные
дают 1 и разоблачают знак.

### 3.3. Прогон чужого кода на своей машине

Отдельное требование брифа: сверяться не только с числами статьи, но и с поведением
`certification.py` на этой машине. План:
1. Запустить `certification.py` авторов в отдельном venv (`.venv-authors`), не в нашем,
   чтобы их зависимости не влияли на наш конвейер.
2. Сверить три вещи: (i) его вывод против его же зафиксированных CSV в репозитории;
   (ii) его вывод против нашего конвейера; (iii) его CSV против чисел статьи.
3. Любое расхождение из трёх записывается в `REPORT.md` §«сверка с чужим кодом» **до**
   того, как начнётся наша собственная работа по n=9.

Ожидание [СЛЕПОЕ]: пункт (iii) уже частично провален — расхождение по числу рёбер
рангов 4/5 между таблицей статьи и CSV найдено чтением (S2.4). Ожидаю, что (i) сойдётся
в пределах τ_bulk (их SCS против их же CSV), а (ii) — в пределах τ_gate.

---

## 4. Блок 0.b — отсев до SDP

### 4.1. Что реализуется

Распознавание совершенных графов через отсутствие индуцированных нечётных дыр и антидыр
длины ≥ 5: для n ≤ 9 достаточно проверить наличие индуцированных C₅, C₇, C₉ в G и в Ḡ
(длины 5, 7, 9 — единственные нечётные ≥5, помещающиеся в 9 вершин). Перебор подмножеств:
C(9,5)+C(9,7)+C(9,9) = 126+36+1 = 163 подмножества на граф, с проверкой «индуцированный
подграф есть цикл».

Для совершенного графа α = ϑ, значит Δ = 0 и SDP не нужен.

### 4.2. Гейт отсева и его честная формулировка

Бриф: «отсев обязан выбросить ровно те графы, у которых полный конвейер даёт Δ < 1e-9,
и ни одного лишнего», сверка пересчётом всех 11 117 обоими путями.

Это двустороннее условие, и стороны у него **не равноценны**, что фиксируется до счёта:

- **B-sound (обязательно).** Ни один граф с Δ > τ_zero не отсеян. Нарушение — фатально:
  оно означает потерю кандидата. Ожидание [ИЗ ТЕОРИИ]: выполнится точно, 0 нарушений,
  потому что совершенность влечёт ϑ=α.
- **B-complete (проверяется, но не обязательно).** Каждый граф с Δ < τ_zero отсеян.
  Ожидание [СЛЕПОЕ]: **ожидаю нарушения**, то есть ожидаю найти несовершенные графы
  на 8 вершинах с ϑ = α. Причина: равенство ϑ(G)=α(G) для *одного* графа не эквивалентно
  совершенности — совершенность есть равенство для *всех* индуцированных подграфов.
  Достаточное условие ϑ=α даёт уже сэндвич α ≤ ϑ ≤ χ(Ḡ) (S1.2) при χ(Ḡ)=α, а такое
  бывает и у несовершенных графов.

**Решение, принятое до счёта:** отсев применяется как *звуковой префильтр* — он только
пропускает SDP там, где Δ=0 доказано; всё, что он не распознал, идёт через SDP. При таком
применении нарушение B-complete стоит машинного времени и ничего больше, а B-sound
остаётся обязательным. Если B-complete нарушится, литеральный гейт брифа считается
проваленным, это печатается в `REPORT.md`, и там же печатается число нарушителей и
несколько их кодов graph6 — как самостоятельный результат.

**Дополнительная страховка, за пределами брифа.** На n=9 SDP гоняется по **всем** 261 080
графам, без применения отсева к боевому прогону. Отсев считается параллельно и сверяется
с полным результатом — то есть гейт 4.2 проверяется и на n=8, и на n=9. Причина: полный
прогон по оценке укладывается в десятки минут на 8 ядрах, и цена страховки ничтожна
против цены незамеченного ложного отсева.

**Какая именно ошибка провалила бы этот тест.** (1) Проверка «есть цикл» без
индуцированности — тогда C₅ найдётся в любом графе с достаточным числом рёбер, отсев
станет почти пустым, B-complete рухнет массово. (2) Забыть антидыры (не проверять Ḡ) —
несовершенные графы вроде C̄₇ будут объявлены совершенными, и B-sound упадёт: у них
Δ>0. Именно C̄₇ на 7 вершинах — минимальный тест этой ошибки, и он входит в набор.

### 4.3. Отчётность

Печатается: сколько графов снимает отсев на n=8 и на n=9, в штуках и процентах, и
оценка сэкономленного времени.

---

## 5. Блок 0.c — перебор n=9

- Вход: `sources/mckay_graph9c.g6`, 261 080 строк (S3, проверено выгрузкой).
- α — точно (§R даёт два независимых пути).
- ϑ — SDP, массовый солвер; топ-100 по Δ пересчитывается вторым солвером.
- Фиксированный сид: `--seed 20260819`; сид влияет только на случайные тесты §R и на
  рестарты η_d, сам перебор детерминирован. Воспроизводимость — одной командой
  `runners/run_stage0.sh`.
- На каждый граф сохраняется: graph6, |E|, α, ϑ, Δ, время решения, статус солвера,
  первичная невязка.
- Непрерывная запись на диск: результаты пишутся чанками в `results/n9_partial_*.csv`,
  прогон возобновляем; обрыв стоит не более одного чанка.
- **Контроль точности:** для топ-100 по Δ второй солвер; расхождение больше τ_hi —
  разбирательство поимённо, не усреднение. Записывается до счёта: усреднение двух
  значений ϑ запрещено.

**Предсказания [СЛЕПОЕ]:**

| # | величина | предсказание | обоснование |
|---|---|---|---|
| C1 | max Δ на n=9 | > 0.46784373 | монотонность 0.236 (n=5) → 0.318 (n=7) → 0.468 (n=8); на 9 вершинах доступны все конструкции с 8 плюс вершина |
| C2 | C₉ не максимизатор | Δ(C₉) = 0.3600896 < max | на n=8 семейство циклов уже проиграло (S1.5); цикл C₉ даёт 0.3600896 против 0.46784, достигнутых уже на 8 вершинах |
| C3 | α максимизатора | 3 или 4 | у всех топ-6 на n=8 α=3 (S1.5); на 9 вершинах разреженный оптимум скорее даст 4 |
| C4 | \|E\| максимизатора | 10–14 | оптимум на n=8 имел 10 рёбер при 11 117 графах; плотные графы в топ-10 n=8 появляются только при α=2 |
| C5 | число графов с Δ > 0.46784373 | ≥ 1 | следует из C1 |

C1 — главное слепое предсказание стадии. Оно **может провалиться**: связность обязательна,
а добавление девятой вершины к Quad-C₅ увеличивает и α, и ϑ, и знак изменения Δ не
очевиден. Если C1 провалится, максимум на 9 вершинах окажется ниже, чем на 8, и это
будет самостоятельным результатом (зазор не монотонен по n).

---

## 6. Блок 0.d — структурный вопрос

### 6.1. Что считается для топ-50

1. **Число индуцированных C₅** и матрица их попарных пересечений по рёбрам: для каждой
   пары пятиугольников — сколько рёбер общих; для каждого ребра — в скольких
   пятиугольниках оно лежит. Величина «покрытие»: распределение кратностей рёбер.
   Эталон n=8 (S1.7): 4 пятиугольника, каждое из 10 рёбер ровно в двух.
2. **\|E\| против ранга по Δ** — коэффициент ранговой корреляции Спирмена по топ-50.
   Проверяемое наблюдение авторов (S1.8) — что оптимум разреженнее эталона — сделано
   ими на **одном** сравнении, а не как тренд; перенос в тренд есть наша конструкция и
   так и помечается.
3. **Степенная последовательность** каждого из топ-50.
4. **d\*** по S1.3 для топ-50: η_3 и η_4 многократным рестартом с фиксированного сида;
   d\*, опирающийся на ненайденное превышение, помечается †.

### 6.2. ПРЕДРЕГИСТРИРОВАННЫЙ ВОПРОС

**Формулировка:** окажется ли максимизатор Δ на 9 вершинах составленным из
перекрывающихся индуцированных C₅ — в том же смысле, в каком это верно на 8 вершинах
(S1.7: четыре индуцированных C₅, каждое ребро ровно в двух)?

**Операционализация, зафиксированная до счёта.** Разделяется на три уровня, чтобы ответ
не зависел от того, насколько щедро толковать «перекрывающиеся пятиугольники»:

- **D1 (слабый).** Максимизатор содержит хотя бы один индуцированный C₅.
- **D2 (средний).** Максимизатор содержит ≥ 2 индуцированных C₅, и хотя бы два из них
  делят хотя бы одно ребро.
- **D3 (сильный, буквальный аналог n=8).** Каждое ребро максимизатора лежит ровно в двух
  индуцированных C₅, и индуцированные C₅ покрывают все рёбра.

**ОЖИДАНИЕ (записано как ожидание, не как предсказание):**

- D1 — **да**. Основание: C₅ — минимальная структура с положительным зазором, и в топ-6
  на n=8 разреженные лидеры все построены вокруг пятиугольников (S1.7, S1.11).
- D2 — **да**, с меньшей уверенностью.
- D3 — **нет**. Основание арифметическое, и оно записано до счёта: D3 требует
  5·(#C₅) = 2·|E|, то есть |E| кратно 5 и #C₅ = 2|E|/5. На n=8 это выполнилось при
  |E|=10, #C₅=4. Требовать того же на 9 вершинах — значит требовать попадания в
  делимость, для которой нет причины. Ожидаю «пятиугольники есть и перекрываются, но
  идеального двукратного покрытия нет».

**Против чего это ожидание проверяется в первую очередь, и почему оно может быть
целиком неверным.** Схема «5, 8, 9» из брифа проходит мимо n=7: там максимизатор — C₇
(S1.4), в котором индуцированных C₅ **ноль**. То есть уже известная последовательность
максимизаторов не является последовательностью «пятиугольных» графов: C₅ (тривиально
пятиугольник) → C₇ (ни одного пятиугольника) → Quad-C₅ (четыре). Это записано до счёта
как главный довод против того, что структура пятиугольников есть закономерность, а не
частность восьми вершин. Если на n=9 D1 провалится, вывод брифа «конструкция Quad-C₅ не
продолжается» получит уже второе подтверждение, а не первое.

**Что считается ответом.** В `REPORT.md` печатается: значение D1/D2/D3 для максимизатора,
те же три значения для всего топ-50 (сколько из 50 удовлетворяют каждому уровню), и
вердикт по каждому уровню против ожидания выше.

**Запрет на спасение.** Если D3 провалится, новый уровень «D2.5» после просмотра данных
не вводится. Любая структура, замеченная в данных и не перечисленная выше, попадает в
`REPORT.md` как наблюдение для следующей стадии, явно помеченное «post hoc».

---

## 7. Блок 0.e — чего не делается

Зафиксировано до счёта, чтобы не расползлось: не доказывается общий критерий; не
трогается n=10; не вводятся взвешенные версии зазора. Стадия закрывается таблицей топ-50
и ответом на 0.d.

---

## R. Правило репозитория: тесты на случайных входах

Каждый оценщик гоняется на случайных графах, а не только на калибровочных.
Сид `20260819`, 500 случайных графов: n ~ U{5..9}, p ~ U(0.1, 0.9), только связные.

| тест | что сверяется | ошибка, которую ловит |
|---|---|---|
| R1 | α двумя путями: битовая маска по всем 2ⁿ подмножествам ↔ `nx.max_weight_clique` на дополнении | ошибка в дополнении, ошибка в переборе |
| R2 | сэндвич α ≤ ϑ ≤ χ(Ḡ) (S1.2), χ считается точным перебором раскрасок | ошибка масштаба и знака в ϑ; путаница G/Ḡ |
| R3 | ϑ двумя солверами (CLARABEL ↔ SCS), расхождение ≤ τ_bulk | сходимость солвера на вырожденных задачах |
| R4 | ϑ третьим солвером (CVXOPT) на подвыборке 50 графов | системная ошибка формулировки, одинаковая у двух солверов |
| R5 | первичная допустимость: max\|X_ij\| по рёбрам и \|Tr X − 1\| | молчаливая недопустимость решения |
| R6 | инвариантность к перенумерации: ϑ и α на случайной перестановке вершин | ошибка индексации, невидимая на вершинно-транзитивных графах |
| R7 | ϑ(пустого) = n, ϑ(полного) = 1 на случайных n | масштаб |

R6 — единственный тест, ловящий баг индексации: все калибровочные графы 0.a, кроме
Quad-C₅, вершинно-транзитивны, и на них перенумерация не видна.

**Гейт R:** любой из R1–R7 падает — 0.c не запускается.

---

## 8. Порядок исполнения и что чем блокируется

```
SOURCES.md + PREREGISTRATION.md + SHA-256 + commit   ← до первого сэмпла
   └─ R (случайные входы)         гейт R
        └─ 0.a калибровка          ГЕЙТ 0.a (τ_gate = 1e-6)
             ├─ 0.a-code прогон чужого кода
             └─ 0.b отсев          гейт B-sound
                  └─ 0.c n=9
                       └─ 0.d структура
                            └─ REPORT.md
```

Ни один блок не запускается раньше своего предшественника. Если падает гейт R или
гейт 0.a — 0.c не считается вовсе, и стадия отчитывается заблокированной.

---

## 9. Что уже известно до счёта (чтобы потом не выдать это за находку)

Список фактов, добытых чтением источников 2026-08-19, **до** любого вычисления:

1. Код графа-победителя на n=8 — `` GCQb`o ``, с обратным апострофом; в брифе он записан
   с апострофом `GCQb'o`, что невалидно в graph6 (S1.6).
2. Таблица топ-10 статьи и CSV авторов расходятся в порядке рангов 4 и 5 (S2.4).
3. README репозитория авторов ссылается на каталог `data/`, которого в репозитории нет
   (S2.2).
4. Архив Zenodo побайтово совпадает с GitHub на коммите `bfacfd0` (S2.1).
5. Числа 853 / 11 117 / 261 080 подтверждены собственной выгрузкой базы Маккея (S3).
6. Замкнутой формы ϑ(Quad-C₅) в статье нет; попытка PSLQ там названа ложноположительной
   (S1.9).
7. Значения d\*=4 у авторов — нижние оценки эвристики, не доказательства (S1.9).
8. Δ(Вагнер) = 0.41421356 из CSV численно совпадает с √2 − 1 = 0.4142135624, то есть
   ϑ(Вагнер) численно равно 2 + √2. **В источниках это не утверждается**; проверяется
   нами как тождество-кандидат с допуском τ_hi и помечается как наше наблюдение, а не
   как известный результат.
