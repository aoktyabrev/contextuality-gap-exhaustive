# REPORT — QUADC5 Stage 8-bis: усиленный поиск и калибровка метода

## Результат: исход B

Цель **(12,5) не найдена** при удесятерённом бюджете; **все контроли найдены**. По печати
`PREREGISTRATION_STAGE8B.md` (`a12ffc5`) это **исход B**, и его смысл записан там до счёта:

> Метод работоспособен там, где контрпример существует и сравним по величине превышения.
> Тогда неудача на (12,5) **свидетельствует против правила**: либо D(12,5) = 2/3, либо
> превышение существенно меньше контрольных 0.04–0.11 и лежит вне досягаемости метода.
> Различить эти два подслучая наличными средствами нельзя, и так и будет написано.

**Свидетельство направлено против нашей собственной формулы** `a ≥ max(5, n − 6)` и в
пользу альтернативы `a ≥ max(5, n − 7)`.

---

## 1. Цель

| метод | лучшее | против порога 2/3 | вычислений ϑ | время | |
|---|---|---|---:|---:|---|
| конус | 0.6666667188 | +5.2·10⁻⁸ | 2 121 349 | 4.8 ч | дошёл до конуса |
| отжиг | 0.6666667140 | +4.7·10⁻⁸ | 2 494 040 | 20.3 ч | дошёл до конуса |
| подъём | 0.6336122880 | −3.3·10⁻² | 2 505 211 | 14.7 ч | **не дошёл даже до конуса** |
| | | | **7 120 600** | 39.8 ч | контрпримера нет |

Два метода из трёх упираются ровно в конус и выше не идут. Подъём не достигает и его.

## 2. Контроли — и почему исход читается

| контроль | (n,a) | известное превышение | найдено |
|---|---|---|---|
| K1 | (11,4) | +0.1082 | 3 метода из 3 |
| K2 | (10,3) | +0.0404 | 3 из 3 |
| K3 | (9,2) | +0.0406 | 2 из 2 (конус неприменим) |
| K4 | (13,5) | +0.0419 | 2 из 2, **с холодного старта** |

**10 прогонов из 10.** Инструмент находит превышение всегда, когда за порогом что-то есть,
и делает это за 36…109 тысяч проб при бюджете 300 000 — с запасом.

## 3. Две способности метода, измеренные раздельно

Это не одно умение, и слепота меряется по второму.

| способность | результат |
|---|---|
| «находит превышение порога» | **10 из 10** |
| «находит максимум слоя» | **2 из 8** |

Недоборы систематические. На K2 все три метода застряли в одной точке 0.6671551 при
истинных 0.7071068 — ровно 0.0400 недобора у каждого. На K1 недоборы 0.028, 0.063, 0.081.
Точно попал только K3, самый мелкий контроль.

**Следствие, обязательное к воспроизведению:** любые числа «лучшее найденное» из этих
прогонов **нельзя читать как оценку D(n,a)** — они систематически занижены.

Для нашей задачи нужна первая способность: вопрос стоит «есть ли что-то выше границы
переноса», а не «каков максимум». Метод пригоден для того, чем мы его пользуем.

## 4. Граница измеренной пригодности: n ≤ 13

Все десять успехов лежат при n ≤ 13. При n = 14 (дополнение, блок ниже) оба метода с
холодного старта **не взяли даже известную планку**. Пригодность на четырнадцати вершинах
не показана и вперёд не переносится.

## 5. Дополнение: третья различающая точка (14,7)

Печать `PREREGISTRATION_STAGE8B_ADDENDUM.md` (`d55fcfb`). Порог 2/3, тот же перенос из (9,3).

| метод | лучшее | |
|---|---|---|
| конус | 0.6666667151 | дошёл до конуса |
| подъём | 0.5301102350 | **не дошёл до известного 2/3** |
| отжиг | 0.4678437816 | **не дошёл**, застрял на другом переносе |

**Точка не дала того, ради чего заводилась.** Конус в слой (14,7) попадает, и метод
«конус» нашёл 2/3 сразу — значит значение там заведомо есть и достижимо. Поиск с
холодного старта до него не добрался. Неудача найти нечто выше значения, которого метод
не находит вовсе, свидетельством не является.

Контраст с (13,5) и есть измерение: тот же отжиг нашёл там 0.8206 за 2 189 проб, тот же
подъём — 0.7808. На тринадцати вершинах инструмент работает, на четырнадцати нет.

**Дефект конструкции дополнения, записанный, а не обойдённый:** контроль при n = 14
построить не из чего — порождающего слоя с известным ответом там нет, перебор туда не
доходил.

## 6. Дополнительная проверка вне запечатанных методов

Сертифицированный контрпример при (13,5) разобран удалением по одной вершине: все
тринадцать удалений сохраняют α = 5 и роняют Δ до **0.44…0.58** при пороге 2/3.

Контрпример на тринадцати вершинах **не является надстройкой над контрпримером на
двенадцати** — он ломается от снятия любой вершины, теряя 0.24…0.38 зазора.

Метод не преднзарегистрирован и отчитывается как дополнительная проверка, а не как часть
протокола.

**Заодно проверено, что сам контрпример не конус:** универсальной вершины нет
(максимальная степень 4 при необходимых 12), |Aut| = 1, треугольников нет, обхват 4,
восемь индуцированных C₅ и семь C₇. То есть это новая конструкция, а не перенос,
принятый за находку.

## 7. Итог по точке (12,5): три независимых отрицательных результата

| источник | объём | результат |
|---|---:|---|
| 8.b, три метода | 7.1·10⁵ | не найдено |
| 8-bis, три метода, ×10 | 7.1·10⁶ | не найдено |
| разбор (13,5) удалением вершины | 13 проверок | не получается |

При десяти успехах из десяти на контролях.

## 8. Проверка запечатанных предсказаний

| | предсказано | получено | |
|---|---|---|---|
| P-B-1 | цель будет найдена | **не найдена** | **MISS** |
| P-B-2 | контроли найдены быстрее цели | найдены все, цель нет | HIT |
| P-B-3 | K4 воспроизводится с холодного старта | 0.8206 за 2189 проб | HIT |
| P-B-4 | превышение в [0.02, 0.15] | не разрешено | — |
| P-B-5 | сертификат подтвердит | не разрешено | — |
| P-D-1 | (14,7) не найдётся | не найдено | HIT |
| P-D-2 | свидетельство сложится **за d = 7** | сложилось | **HIT** |
| P-D-3 | лучшее в (14,7) = 2/3 или ниже | 0.6666667 (конус) | HIT |

P-B-1 провален. P-D-2 — предсказание против собственной формулы — **сбылось**.

## 9. Что теперь известно и что нет

**Известно.** H7-S мертва (сертификат, Stage 8). Шестнадцать точек допускают ровно две
формулы. Три независимых отрицательных результата по (12,5) при работающем инструменте.

**Не известно.** Какая из двух формул верна. Исход B оставляет два подслучая, и наличными
средствами они не различаются:

1. D(12,5) = 2/3 — слой наследует, верна `d = 7`;
2. D(12,5) > 2/3, но превышение много меньше контрольных 0.04–0.11 и вне досягаемости
   метода — верна `d = 6`.

**Закрыть вопрос может только полный перебор n = 12** — 164 059 830 476 связных графов,
около 490 суток при измеренной пропускной способности. Не планируется.

**Половина правила по-прежнему стоит на одном наблюдении.** Точек, где шестёрка реально
проверяется (a ≥ 5 и n − a ≥ 7), ровно одна — сертифицированный контрпример (13,5).
Остальные пятнадцать с ней согласны, но её не проверяют. Это записано в
`PREDICTION_INHERITANCE_RULE.md` §оговорки и здесь повторено, потому что три отрицательных
результата этого не меняют.
