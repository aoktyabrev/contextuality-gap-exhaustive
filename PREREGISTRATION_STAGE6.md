# PREREGISTRATION — QUADC5, Stage 6 (потолок ϑ и n = 11)

Написано **до первого прогона стадии**. После того как хэши из
`PREREGISTRATION_STAGE6.sha256` закоммичены, файл не редактируется.

Дата: **2026-08-20** (UTC).

## 0. Поправка к состоянию кампании, из брифа

Письмо авторам arXiv:2605.12828 **отправлено** автором лично 2026-08-20 в 19:02, всем
четверым. Переписыванию не подлежит. Не отправлено другое письмо — Л. Э. Даниэльсену
(Берген) с запросом копии файлов базы, недостижимых с 2022 года (Stage 3, S11).
Это фиксируется в `REPORT_STAGE6.md`, чтобы состояние кампании было верным.

## 1. Определение α\*, фиксируется здесь

**Дробное число упаковки** графа G:

> α\*(G) = max Σ_v w_v при условиях Σ_{v ∈ C} w_v ≤ 1 для каждой клики C графа G, w ≥ 0.

По двойственности ЛП это в точности дробное число покрытия кликами, оно же дробное
хроматическое число дополнения: α\*(G) = χ_f(Ḡ). Цепочка, на которой стоит блок:
**α(G) ≤ ϑ(G) ≤ α\*(G)**.

### 1.1. Расхождение с калибровкой, указанной в брифе — записано до счёта

Бриф даёт для калибровки α\*(C₅) = 5/2 и **α\*(C₇) = 7/3**. Второе значение
относится к другой величине.

- Для C₇ максимальные клики — это рёбра, ограничения имеют вид w_i + w_{i+1} ≤ 1,
  и равномерный вес ½ допустим: **α\*(C₇) = 7/2**.
- Величина **7/3 — это дробное хроматическое число самого C₇**, χ_f(C₇) = 7/3, то есть
  χ_f(G), а не χ_f(Ḡ).
- Проверка сэндвичем решает вопрос однозначно: ϑ(C₇) = 3.3177 обязано быть ≤ α\*(C₇).
  Значение 7/2 = 3.5 это допускает, значение 7/3 = 2.333 — нет.
- У C₅ обе величины совпадают (5/2), потому что C₅ самодополнителен; поэтому
  расхождение видно только на семёрке.

**Калибровка поэтому ведётся против 5/2 и 7/2**, а χ_f(G) печатается рядом как
контрольная величина, чтобы разница была видна в отчёте. Отклонение от брифа объявлено
до счёта.

## 2. Что известно на момент написания

Всё содержимое стадий 0–5. Отдельно, и это важно для честности блока 6.a:

**В ходе Stage 0 я считал χ_f(Ḡ) для девятивершинного максимизатора инлайновым скриптом
и получил 4 при ϑ = 11/3.** Это число **не было занесено ни в один отчёт и ни в один
раннер**, следовательно по правилу 0 оно не является источником — это непроверенное
воспоминание о промежуточном счёте. Оно помечается **UNVERIFIED** и перепроверяется в
6.a точной рациональной арифметикой. Записано здесь до счёта, чтобы предсказание H1
ниже не выглядело угаданным задним числом.

## 3. Блок 6.a — потолок

### 3.1. Метод: точно, а не численно

α\* рационально, поэтому численное решение не годится. Схема — та же, что везде в
проекте: численное решение поставляет кандидата, а засчитывается **сертификат**.

1. ЛП решается численно (scipy/HiGHS) по максимальным кликам G;
2. примальное решение w и двойственное y рационализируются;
3. проверяется **точно, в дробях**: w ≥ 0 и Σ_{v∈C} w_v ≤ 1 для каждой клики
   (даёт α\* ≥ Σw); y ≥ 0 и Σ_{C ∋ v} y_C ≥ 1 для каждой вершины (даёт α\* ≤ Σy);
4. если Σw = Σy — значение доказано и печатается дробью.

Достаточно ограничений по **максимальным** кликам: ограничения по остальным следуют.

### 3.2. Предсказания

| # | предсказание | обоснование | что считается подтверждением |
|---|---|---|---|
| **H1** | **гипотеза брифа неверна: у девятивершинного максимизатора ϑ ≠ α\***, и α\* = 4 при ϑ = 11/3, то есть зазор ровно **1/3** | воспоминание о счёте Stage 0 (UNVERIFIED, §2); плюс α\* целое здесь правдоподобно, поскольку 4 = α + 1 | точное α\* = 4 в дробях и сертификат |
| H2 | калибровка даст α\*(C₅) = 5/2 и α\*(C₇) = **7/2** | §1.1 | точное совпадение в дробях |
| H3 | зазор α\* − ϑ будет **ненулевым у всех шести максимизаторов** | если он ноль где-то, это само по себе результат; но оснований ждать нуля нет ни на одном размере | — |
| H4 | графы с ϑ = α\* существуют и это в основном **совершенные графы**, где α = ϑ = α\* тривиально | у совершенного графа χ_f(Ḡ) = χ(Ḡ) = ω(Ḡ) = α, и вся цепочка схлопывается | — |

**H1 — предсказание против гипотезы брифа.** Гипотеза брифа принадлежит автору, и я
записываю, что ожидаю её провала, вместе с основанием. Если она подтвердится — промах
мой, и он будет отмечен как промах.

### 3.3. Обратное направление (пункт 4 брифа)

Независимо от исхода H1 собираются **все** графы n ≤ 10 с ϑ = α\* — проверка точная — и
смотрится их положение в рейтинге по Δ. Вопрос: является ли достижение потолка признаком
высокого зазора или свойством вырожденных случаев. **Ожидание:** признаком не является,
и подавляющее большинство таких графов имеет Δ = 0 (см. H4).

## 4. Блок 6.b — рациональность вообще

Собираются все графы n ≤ 10 с рациональным ϑ — проверка точная, через целочисленное
соотношение степени 1 по правилу запаса знаков `PREREGISTRATION_STAGE2` §3.1, а не по
виду десятичной записи. Разведка; отрицательный ответ допустим.

**Ожидание:** ничего общего, кроме уже известного из Stage 2 и 5 — связи с симметрией нет
(ρ = −0.116 на 42 графах), связи со слоем нет (атом √5 − 2 сквозной).

## 5. Блок 6.c — n = 11 как проверка запечатанного предсказания

Предсказание `PREDICTION_N11.md` (коммит `9b833a2`): **Δ_max(11) ∈ [0.7475, 0.9059]**,
доминирующий слой a = 3, α максимизатора равно 3.

**Предсказание не меняется ни при каком исходе** (бриф 6.d). Его провал — такой же
результат, как попадание, и записывается так же.

### 5.1. Оценка времени, до запуска

Замеры n = 10: фильтрующий проход 0.18 мс на граф, SDP 7 мс, отсев снимает 91.67 %,
полный прогон 1542 с на 7 процессах. Для n = 11 при 1 006 700 565 графах:

- фильтрующий проход: 0.18 мс × 1.0067e9 = 181 000 с ÷ 7 ≈ **7.2 часа**;
- SDP на выживших: доля выживших при n = 10 была 8.33 %; для n = 11 берётся тот же
  порядок, 8–10 %, то есть 80–100 млн задач по ≈ 8.5 мс (рост как (11/10)³) =
  680 000–850 000 с ÷ 7 ≈ **27–34 часа**.

**Предсказание T-11: полный перебор n = 11 займёт 34–41 час на 7 процессах.**

Это на порядок больше, чем оценка «около пяти часов», записанная в `PREDICTION_N11.md`:
та оценка ошибочно масштабировала только число графов, не учтя, что фильтрующий проход
тоже линеен по нему, а доля выживших не падает. Ошибка признаётся здесь; сама она
предсказания Δ_max(11) не касается и его не меняет.

**Следствие, записанное заранее:** если фактическое время выйдет за 48 часов, перебор
останавливается, и стадия отчитывается с частичным результатом по тем частям `res/mod`,
которые успели пройти, с явным указанием охвата. Частичный перебор **не** может
опровергнуть предсказание (максимум мог остаться в непройденной части), но может его
подтвердить, если найденное значение уже попадает в интервал и слой a = 3 лидирует.

### 5.2. Что делается по завершении

Топ-100 вторым солвером; точные сертификаты для топ-5; проверка выводов 4.b (содержат ли
лидеры одиннадцати вершин десятивершинный оптимум индуцированным подграфом) и 4.c
(продолжается ли какая-либо конструкция).

## 6. Чего не делается

Не n = 12. Предсказание для n = 11 не меняется. Гипотеза 6.a не спасается в случае
провала — записывается как отрицательный результат с фактическими зазорами.
