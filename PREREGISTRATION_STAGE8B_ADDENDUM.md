# PREREGISTRATION — дополнение к 8-bis: третья различающая точка (14,7)

Запечатывается **2026-08-25**, до запуска. Дополняет `PREREGISTRATION_STAGE8B.md`
(печать `a12ffc5`), не изменяя его: цели, контроли и бюджеты той печати остаются как есть.

## 0. Повод

Блок 8-c установил, что шестнадцать точек допускают **ровно две** формулы вида
`a ≥ max(c, n − d)`: наша `d = 6` и альтернатива `d = 7`. Они расходятся **не в одной
точке**, а на всём семействе `a = n − 7`, то есть при разности n − a = 7:

| n | a | порог T(n,a) | источник | d = 6 | d = 7 | что уже есть |
|--:|--:|---|---|---|---|---|
| 12 | 5 | 2/3 | (9,3) | порождает | наследует | 8-bis, ×10, идёт |
| 13 | 6 | 2/3 | (9,3) | порождает | наследует | 8.b: не найдено (0.5508) |
| **14** | **7** | **2/3** | (9,3) | порождает | наследует | **не проверялось** |

Порог у всех трёх один и тот же — 2/3, перенос из девятивершинного максимизатора.

## 1. Что добавляется

Одна цель **(14, 7)**, порог 2/3, теми же тремя методами, бюджет **3·10⁵ вычислений ϑ
на пару** — как у контролей 8-bis, а не как у усиленной цели. Обоснование: точка нужна
для набора согласованных свидетельств, а не для решающего одиночного вердикта.

Контроли не дублируются: они уже отработали в 8-bis, все четыре найдены.

## 2. ЧТО БУДЕТ ОЗНАЧАТЬ ИТОГ ПО ТРЁМ ТОЧКАМ

Записано до счёта.

**Если контрпример найден хоть в одной из трёх** — формула `d = 7` мертва, `d = 6`
выживает. Одной находки достаточно: она сертифицируется и становится фактом.

**Если ни в одной не найдено, а контроли 8-bis найдены все четыре** — три независимых
отрицательных результата при работающем инструменте. Это **свидетельство в пользу
`d = 7`**, то есть против нашей же формулы. Слабое по отдельности, но согласованное:
слепота метода должна была бы сработать трижды подряд именно на различающих точках и ни
разу на контрольных.

**Если ни в одной не найдено и хоть один контроль в 8-bis провалился** — вывода нет,
и это записывается как отсутствие вывода.

## 3. ЗАПЕЧАТАННЫЕ ПРЕДСКАЗАНИЯ

**P-D-1.** В (14,7) контрпример **не будет найден**.

**P-D-2.** Итог по трём точкам окажется «ни в одной не найдено при всех найденных
контролях», то есть свидетельство сложится **в пользу `d = 7`** и против нашей
собственной формулы `a ≥ max(5, n − 6)`.

**P-D-3.** Лучшее найденное в (14,7) окажется **равно 2/3 с точностью до шума солвера**
или ниже: поиск упрётся в сам конус, как это было в (12,5) и (13,6).

### Что считается провалом
- P-D-1: контрпример найден ⇒ `d = 6` подтверждена, и это переворачивает вывод.
- P-D-2: итог иной.
- P-D-3: лучшее заметно ниже 2/3 при том, что конус в этот слой попадает.

## 4. Оговорка, обязательная к воспроизведению в отчёте

Три отрицательных результата **не доказывают** `d = 7`. Они говорят лишь, что при
работающих контролях метод трижды не нашёл там, где по `d = 6` находка обязана быть.
Полный перебор n = 12 (1.64·10¹¹ графов) остаётся единственным способом закрыть вопрос,
и он не планируется.
