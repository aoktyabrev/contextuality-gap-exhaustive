# PREDICTION — правило наследования: a ≥ max(5, n − 6)

Записано **2026-08-25**, после того как контрпример к H7-S найден и сертифицирован, и
**до** какого-либо счёта, проверяющего настоящее правило. Запечатывается в
`PREDICTION_INHERITANCE_RULE.sha256`.

## Что случилось с прежней формулировкой

H7-S утверждала равенство D(n, a) = T(n, a) при a ≥ a\*, и граница a\* = 5 держалась на
полных переборах n = 9, 10, 11. Она **опровергнута** сертификатом: граф
`` L@JC?_ASKAGPBH `` на 13 вершинах, 19 рёбер, α = 5, имеет

    Delta in [0.816758669642764, 0.816758669767688]   (доказано, ширина 1.2e-10)

против границы переноса T(13,5) = Δ_max(11) ≤ 0.7748885327466875 (доказано, Stage 7.1b).
Превышение ≥ 0.0419, сравнение между двумя точными вилками, без плавающей точки.

## Что предлагалось взамен и почему это не подошло

Гипотеза «граница сидит в разности n − a» **не проходит**: при n − a = 5 слой (9,4)
порождает своё, при n − a = 6 — слои (9,3) и (10,4). Одной разности мало.

## ПРАВИЛО

> **Слой (n, a) наследует тогда и только тогда, когда a ≥ max(5, n − 6).**

Эквивалентно: нужны **оба** условия — a ≥ 5 **и** n − a ≤ 6.

На всех имеющихся данных — 15 точек из полных переборов n = 9, 10, 11 плюс
сертифицированный контрпример (13,5) — **исключений ноль**.

| n − a | наследуют | порождают своё |
|--:|---|---|
| 4 | (9,5) (10,6) (11,7) | — |
| 5 | (10,5) (11,6) | (9,4) |
| 6 | (11,5) | (9,3) (10,4) |
| 7 | — | (9,2) (10,3) (11,4) |
| 8 | — | (10,2) (11,3) **(13,5)** |
| 9 | — | (11,2) |

Прежнее «a ≥ 5» совпадало с правилом на n = 9, 10, 11 случайно: там max(5, n − 6) = 5.
Расхождение начинается ровно с n = 12.

## ПРЕДСКАЗАНИЯ

**P-R-1.** При n = 12 наследуют слои a ≥ 6; слой **a = 5 порождает собственное значение**,
то есть D(12, 5) > T(12, 5) = 2/3.

**P-R-2.** При n = 13 наследуют слои a ≥ 7; слои a = 5 и **a = 6** порождают своё,
то есть D(13, 6) > T(13, 6) = 2/3. Для a = 5 это уже установлено сертификатом.

**P-R-3.** Наше собственное запечатанное предсказание `PREDICTION_N12_LAYERS.md`
(коммит `490ab59`) **провалится ровно в одной строке из четырёх**: слой 5 (там
n − a = 7). Строки для слоёв 6, 7 и 8 (n − a = 6, 5, 4) уцелеют.

**P-R-4.** Превышение в слоях с n − a = 7 будет **того же порядка**, что наблюдалось на
полных переборах при n − a = 7: +0.040 при n = 9, +0.040 при n = 10, +0.108 при n = 11.
Предсказывается D(12,5) − 2/3 ∈ [0.02, 0.15].

**P-R-5. Поиск 8.b даёт ложноотрицательные результаты, и это измеримо.** Он покрыл
(12,5) и (13,6) — обе точки, по правилу, порождающие своё, — суммарно ~1.17·10⁶
вычислений ϑ пятью прогонами, и не нашёл ничего. Предсказывается, что при направленном
доборе бюджета или лучшем старте контрпримеры в этих двух точках **найдутся**, то есть
отрицательный результат 8.b в них был ложным.

### Что считается провалом

- P-R-1 или P-R-2: указанный слой окажется равен переносу с точностью 10⁻⁶.
- P-R-3: провалится не одна строка, а другое их число.
- P-R-4: превышение вне [0.02, 0.15].
- P-R-5: усиленный поиск в (12,5) и (13,6) снова ничего не найдёт ⇒ либо правило
  неверно, либо метод систематически слеп, и различить это будет нельзя.

## Оговорки, записанные вместе с правилом

1. **Две подогнанные константы на шестнадцати точках.** Пятёрка и шестёрка выбраны по
   данным. Это подгонка, а не вывод; механизма за правилом не предлагается.
2. **Асимметрия свидетельств.** Все шестнадцать точек — из размеров n ≤ 11 плюс одна при
   n = 13. Правило экстраполирует на n = 12 и 13, где полных данных нет.
3. **Часть «a ≥ 5» может быть артефактом малых размеров** ровно так же, как им оказалась
   вся прежняя формулировка a\* = 5. Если при n = 14 или 15 нижний порог сдвинется,
   правило придётся переписывать снова, и это будет третья редакция.
4. **Слабая форма H7 не затронута.** D(n, a) ≥ T(n, a) остаётся доказанной теоремой; всё
   вышесказанное относится только к равенству.
