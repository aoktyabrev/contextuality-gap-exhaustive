# PREREGISTRATION — QUADC5 Stage 7: гипотеза наследования верхних слоёв

Запечатывается **2026-08-22**, до первого прогона проверки. Хэши — в
`PREREGISTRATION_STAGE7.sha256`; коммит печати предшествует любому коммиту с
результатами стадии, как в Stage 0–6. Действуют стандартные правила §0
`PREREGISTRATION.md` — Rule 0, калибровочное, репозиторное, kill-правило, «тест, который
не может провалиться, ничего не проверяет».

Stage 6 на момент печати **не закрыта**: перебор n = 11 идёт. Настоящая стадия его не
трогает и от него не зависит.

---

## 0. Происхождение гипотезы и почему это ослабляет стадию

Записать это честно важнее, чем записать красиво.

**Гипотеза сформулирована после того, как данные увидены.** Наблюдение — совпадение
максимумов верхних слоёв при n = 11 с максимумами меньших размеров до 10⁻⁷ — получено в
рабочем прогоне 2026-08-22 и записано в `REPORT_STAGE6.md` **без механизма**, явно как
наблюдение для будущей стадии. Механизм (конструкция переноса) сформулирован ассистентом
(Claude) в диалоге 2026-08-22, уже после наблюдения.

Следствие, принимаемое без спора: **данные n = 11 проверкой H7 не являются** и в вердикт
стадии не входят ни для подтверждения, ни для опровержения. Проверка идёт на n = 9 и
n = 10, где полный перебор закрыт и данные не менялись с 2026-08-18.

**Второе ослабление, которое бриф не назвал, а оно есть.** Данные n = 9 и n = 10 не
только закрыты — они частично **опубликованы**, и на момент печати автору этой
предрегистрации видны. `REPORT_STAGE5.md` блок 5.c публикует D(n, a) для a = 2, 3, 4 при
n = 5…10; `PREDICTION_N11.md` публикует, сверх того, слой a = 5 при n = 9 и 10. То есть
значительная часть того, что бриф предлагает «предсказать», уже известна.

Поэтому предсказания ниже разделены на две группы, и смешивать их нельзя:

- **Уже разрешённое** (§2) — записывается как констатация, а не как предсказание. Печать
  на известном ответе была бы театром и обесценила бы все прежние печати.
- **Запечатанное** (§4) — только то, что на момент печати **не вычислено и не
  опубликовано**: слои a ≥ 6 при n = 9 и n = 10, точная граница наследования, и исход
  аналитического блока 7.a.

Чтобы вторая группа осталась настоящей, `results/report_5a.json` **до печати не
открывался**; использованы только два опубликованных отчётных документа, названных выше.

---

## 1. Гипотеза

Пусть G\* — граф на m вершинах с α(G\*) = a₀ и Δ(G\*) = ϑ(G\*) − a₀.

**Конструкция переноса (конус над присоединением независимых вершин).** К G\* добавляются
k вершин: k − 1 вершин u₁ … u_{k−1}, попарно несмежных и несмежных с G\*, и одна вершина
v, смежная со **всеми** остальными вершинами. Результат обозначается C_k(G\*).

Перебор идёт по **связным** графам, поэтому дизъюнктное объединение недопустимо;
универсальная вершина v — минимальная модификация, возвращающая связность, и она вводится
здесь, до счёта, а не подбирается потом.

**H7 (слабая форма).** C_k(G\*) связен, имеет n = m + k вершин, α = a₀ + k − 1 и то же
Δ. Следовательно для всякого слоя

> **D(n, a) ≥ max{ D(m, a + m + 1 − n) : m < n }**,

где D(n, a) = max{ Δ(G) : G связен на n вершинах, α(G) = a }. Правую часть далее
называем **границей переноса** T(n, a).

**H7-S (сильная форма).** Для верхних слоёв неравенство обращается в равенство: верхние
слои не порождают собственных значений, а наследуют, D(n, a) = T(n, a).

---

## 2. Что уже разрешено опубликованными данными — констатация, не предсказание

Из `REPORT_STAGE5.md` 5.c и `PREDICTION_N11.md`, посчитано до печати:

| n | a | D(n, a) | T(n, a) | источник переноса | равенство |
|--:|--:|---|---|---|---|
| 9 | 2 | 0.3837191 | 0.3431458 | (8, 2) | нет, +0.0405733 |
| 9 | 3 | 0.6666667 | 0.4678437 | (8, 3) | нет, +0.1988230 |
| 9 | 4 | 0.3600896 | 0.3176672 | (7, 3) | **нет, +0.0424224** |
| 9 | 5 | 0.2360680 | 0.2360680 | (5, 2) | **да** |
| 10 | 2 | 0.5000000 | 0.3837191 | (9, 2) | нет, +0.1162809 |
| 10 | 3 | 0.7071068 | 0.6666667 | (9, 3) | нет, +0.0404401 |
| 10 | 4 | 0.5507551 | 0.4678437 | (8, 3) | **нет, +0.0829114** |
| 10 | 5 | 0.3176672 | 0.3176672 | (7, 3) | **да** |

Отсюда три вещи, **известные до печати**:

1. **H7 в слабой форме на этих данных нигде не нарушена**: D ≥ T во всех восьми строках.
2. **Предсказания брифа P7-1 и P7-2 для слоя a = 4 уже ложны.** Бриф предсказывает
   D(9, 4) = Δ_max(7) = 0.3176672 — фактически 0.3600896; и D(10, 4) = Δ_max(8) =
   0.4678437 — фактически 0.5507551. Бриф — источник как любой другой (Rule 0), и здесь
   он расходится с нашими же опубликованными данными. Запечатать эти два утверждения как
   открытые предсказания было бы подлогом.
3. **P7-4 брифа выполняется**: нижние слои a = 2, 3 порождают собственные значения,
   превышающие перенос, на обоих размерах.

**Вывод, фиксируемый здесь и не выносимый на вердикт:** H7-S в форме «равенство для всех
слоёв выше нижних» **ложна** — она ломается на a = 4. Открытым остаётся, где именно
проходит граница и держится ли равенство выше неё.

---

## 3. Блоки

### 7.a — аналитика первой

Доказать или опровергнуть, точно, без обращения к счёту:

1. α(C_k(G\*)) = α(G\*) + k − 1.
2. ϑ(C_k(G\*)) = ϑ(G\*) + k − 1.
3. C_k(G\*) связен для всякого k ≥ 1.

Для (2) требуются два свойства ϑ, каждое из которых доказывается или опровергается
здесь, а не берётся на веру: аддитивность на дизъюнктном объединении и поведение при
соединении (join) с одной вершиной. Оба формулируются и проверяются **численно на
случайных входах двумя независимыми маршрутами** (репозиторное правило) прежде, чем
использоваться в выводе.

Если связная реализация переноса невозможна или ϑ не аддитивна — H7 падает здесь же,
аналитически, и это результат стадии, а не её неудача.

### 7.b — проверка на n = 9 и n = 10 (вердикт)

Для каждого n ∈ {9, 10} и **каждого** слоя a (включая a ≥ 6, ранее не считавшиеся)
выписать D(n, a) из сохранённых полных таблиц `results/n9_all.csv` и
`results/n10_nonzero.csv`, вычислить T(n, a) и сравнить.

Слои с D(n, a) = 0 (пустые или без положительного зазора) отмечаются отдельно и в оценку
равенства не входят: 0 = 0 — тождество, а не наследование.

### 7.c — граница наследования

Найти наименьший слой a\*(n), начиная с которого D(n, a) = T(n, a) для всех a ≥ a\*(n).
Отчитать таблицей: n, a\*(n), число связных графов на n вершинах со слоем a ≥ a\*(n),
доля от полного перебора. Это практический выход: слои выше границы не требуют перебора,
их значения известны из меньших размеров.

Если равенство не образует верхнего хвоста (держится на одних слоях и ломается на более
высоких), границы нет, и это записывается как отсутствие ответа.

### 7.d — литературный чек, строго после счёта

Аддитивность ϑ на дизъюнктном объединении почти наверняка известна (Ловас, 1979) —
**проверять только после того, как собственный результат получен**, и заносить в
`SOURCES.md` дампом с датой обращения. Если свойство известно, оно записывается как
**известное свойство, применённое к новой задаче**, а не как находка стадии. Приоритет
на конструкцию C_k и на границу наследования — отдельный вопрос, решаемый там же.

---

## 4. ЗАПЕЧАТАННЫЕ ПРЕДСКАЗАНИЯ

Только то, что на момент печати не вычислено и не опубликовано.

**P7-α (аналитика).** Все три утверждения 7.a верны: α сдвигается ровно на k − 1, ϑ
сдвигается ровно на k − 1, граф связен. Δ переносится точно, а не приближённо.

**P7-β (верхний хвост).** Для n = 9 и n = 10 равенство D(n, a) = T(n, a) держится для
**всякого** непустого слоя a ≥ 5, включая a ≥ 6, которые ещё не считались.

**P7-γ (граница).** a\*(9) = 5 и a\*(10) = 5. Граница наследования — ровно слой 5 на
обоих размерах, поскольку a = 4 её уже нарушает (§2).

**P7-δ (точность).** Совпадения в P7-β выполняются с расхождением ≤ 10⁻⁶. Порог взят
крупнее шума солвера при n ≤ 10 (измеренный пол ~10⁻⁷ при n = 9) и мельче наименьшего
осмысленного зазора; допуск фиксируется здесь и по ходу не меняется.

**P7-ε (практический выход).** Доля связных графов при n = 10 со слоем a ≥ 5 лежит в
**[5 %, 30 %]**. Оценка получена до счёта и не подсматриванием: почти все графы на
десяти вершинах близки к случайному G(10, ½), а там математическое ожидание числа
независимых пятёрок равно C(10,5)·2^(−C(5,2)) = 252/1024 = 0.246, шестёрок — 210/2¹⁵ =
0.0064, то есть типичное α равно 4, а α ≥ 5 — заметное меньшинство. Отсюда вывод,
записываемый заранее: экономия перебора от H7-S реальна, но перебор нельзя сократить
больше чем на треть, и стадия закончится утверждением о структуре, а не серьёзным
ускорением. `results/alpha_counts.json` до печати не открывался.

### Что считается подтверждением

Все пять сбылись.

### Что считается провалом

- P7-α: любое из трёх утверждений ложно ⇒ H7 падает целиком, включая слабую форму.
- P7-β: хотя бы один непустой слой a ≥ 5 при n = 9 или n = 10 даёт D > T больше чем на
  10⁻⁶ ⇒ H7-S ложна и в ослабленной форме «верхний хвост наследует».
- P7-β в другую сторону: D < T больше чем на 10⁻⁶ ⇒ ложна **слабая** H7, что означало бы
  ошибку в 7.a, и тогда провал P7-α тоже.
- P7-γ: граница не равна 5 хотя бы на одном размере.
- P7-δ: расхождение больше 10⁻⁶ там, где равенство заявлено.
- P7-ε: доля вне [5 %, 30 %].

### Промежуточный исход

Если часть слоёв наследует, а часть нет, и хвоста не образуется — записывается как есть:
H7-S ложна, H7 в слабой форме выживает, границы наследования не существует. Это
полноценный результат стадии, а не её провал.

---

## 5. Чего не делается

- **Не использовать данные n = 11 в вердикте** — ни для подтверждения, ни для
  опровержения. Совпадения при n = 11 остаются наблюдением, породившим гипотезу.
- **Не считать n = 12.**
- **Не расширять гипотезу на другие конструкции переноса** до закрытия этой. C_k
  зафиксирована в §1 и по ходу не меняется.
- **Не искать в литературе подтверждение до счёта** (см. 7.d).
- **Не вводить новых величин после того, как данные 7.b увидены.** Если обнаружится
  что-то интересное сверх H7, оно записывается как наблюдение для следующей стадии, без
  механизма — ровно так, как поступили с породившим эту стадию наблюдением.
- Не трогать `PREDICTION_N11.md`, `PREDICTION_EDGES.md` и открытую Stage 6.

## 6. Стоп-правило

Стадия целиком считается по сохранённым таблицам n = 9 и n = 10; нового перебора нет,
времени это занимает минуты. Если 7.a опровергнет конструкцию, 7.b всё равно
выполняется — таблица D против T интересна независимо от того, объясняется ли она
конусом.
