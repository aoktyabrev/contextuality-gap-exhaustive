# PREREGISTRATION — QUADC5 Stage 8: атака на верхнюю половину наследования

Запечатывается **2026-08-25**, до первого прогона. Хэши — в
`PREREGISTRATION_STAGE8.sha256`; коммит печати предшествует любому коммиту с
результатами стадии. Действуют стандартные правила §0 `PREREGISTRATION.md`.
Литчек (8.d) выполняется **после** собственных попыток.

---

## 0. Что доказано, что нет, и что стадия пытается сделать

**Доказано** (`REPORT_STAGE7.md`, `OPEN_PROBLEM.md`): конус
C_k(G\*) = {v} ∨ (G\* ⊔ K̄_{k−1}) связен и переносит Δ ровно, откуда

    D(n, a) ≥ Δ_max(m)  при a = a_m + (n − m) − 1,

точное неравенство между алгебраическими числами. Границу переноса по всем m
обозначаем T(n, a).

**Не доказано:** что в слое нет ничего большего, то есть D(n, a) = T(n, a) при a ≥ a\*.
Это измерено на n = 9 и n = 10 (вердикт Stage 7) и наблюдено на n = 11.

**Цель стадии — либо контрпример, либо продвижение к доказательству.** Контрпример
ценнее: он дешевле и убивает гипотезу окончательно.

## 1. Блок 8.a — что уже есть в данных

**Записывается как ФАКТ, не как вердикт.** Данные n = 11 не могут проверять гипотезу,
выросшую из них; это зафиксировано ещё в `PREREGISTRATION_STAGE7.md` и здесь не
пересматривается.

Более того: послойные максимумы при n = 11 **уже вычислялись** по ходу Stage 6 и мне
известны. Никакого предсказания на них не делается и делаться не может. Блок нужен,
чтобы прогнать их через тот же аппарат, что и n = 9, 10 (`runners/run_7.py`), а не
через разовые команды, и получить сопоставимую таблицу.

Отчитывается: держится ли a\* = 5 на трёх размерах подряд, и на сколько слои a ≤ 4
превышают перенос.

## 2. Блок 8.b — направленный поиск контрпримера

Полного перебора нет и не будет. Ищется граф в верхнем слое с Δ строго выше переноса.

### 2.1 Цели, с порогами, вычисленными из установленных ранее величин

| цель | n | слой a | нужно Δ > | то есть ϑ > | перенос из |
|---|--:|--:|---|---|---|
| T1 | 12 | 5 | 0.6666666667 | 5.6666666667 | (9, 3) |
| T2 | 12 | 6 | 0.4678437298 | 6.4678437298 | (8, 3) |
| T3 | 13 | 5 | 0.7748885327 | 5.7748885327 | (11, 4) |
| T4 | 13 | 6 | 0.6666666667 | 6.6666666667 | (9, 3) |

### 2.2 Методы — не менее трёх независимых

Требование независимости здесь содержательное: неудача одного метода не должна
читаться как отсутствие контрпримера.

1. **Локальный подъём.** Старт со случайного связного графа с нужным α; шаг —
   добавление или удаление ребра при сохранении α; принимается, если Δ выросла.
2. **Имитация отжига** по той же окрестности, с убывающей температурой; принимает
   ухудшения с вероятностью exp(−ΔΔ/T), что даёт выход из локальных максимумов.
3. **Возмущение конусов.** Старт с самого конуса C_k(G\*) — он лежит ровно на границе —
   и поиск улучшения малыми изменениями. Это направленно проверяет, является ли конус
   локальным максимумом.

### 2.3 Контроль качества, тот же, что везде

α проверяется **точно** на каждом шаге (перебор по битовым маскам). ϑ считается SDP
с контролем точности. Любой кандидат с Δ выше порога **немедленно**: пересчёт вторым
солвером, затем точный сертификат по машинерии Stage 7.1b. Планка не понижается:
численное превышение порога результатом не считается.

### 2.4 Бюджет, фиксируемый здесь

**На каждую пару (цель, метод): 100 рестартов × 3000 шагов = 3·10⁵ вычислений ϑ.**
Всего 4 цели × 3 метода = **3.6·10⁶ вычислений ϑ**. Прогон останавливается досрочно
только при находке.

### 2.5 Что будет означать «не найдено»

Ровно следующее, и в отчёте будет написано этими словами:

> Контрпримера **не найдено в окрестности поиска**: в 3.6·10⁶ вычислениях ϑ, трёх
> методах и четырёх целях, стартуя со случайных графов и с самих конусов.

Это **не** утверждение об отсутствии контрпримера. Пространство при n = 12 содержит
1.64·10¹¹ связных графов; 3.6·10⁶ проб покрывают его долю порядка 10⁻⁵. Отрицательный
исход усиливает гипотезу слабо и должен подаваться как слабое свидетельство.

## 3. Блок 8.c — доказательство от края

Не общая теорема, а **граница применимости**: доказать D(n, a) = T(n, a) для a ≥ n − c
и найти наибольшее c, при котором получается.

- a = n − 1: разобрать полностью;
- a = n − 2, n − 3: то же, спускаясь;
- явно назвать, на каком c рассуждение ломается и почему.

Доказательства ведутся без обращения к теореме о совершенных графах, которой нет ни в
одном нашем источнике; допустимы сэндвич α ≤ ϑ ≤ χ(Ḡ) (`SOURCES.md` S1.2) и свойства
ϑ из S7.

## 4. ЗАПЕЧАТАННЫЕ ПРЕДСКАЗАНИЯ

**P8-1.** Контрпример **не будет найден** ни в одной из четырёх целей.

**P8-2.** В каждой цели лучшее найденное значение окажется **равно границе переноса**
с точностью до шума солвера, а не ниже её: поиск будет сходиться к самому конусу.
Иначе говоря, конус — локальный максимум в этой окрестности.

**P8-3.** Блок 8.c доведёт доказательство до **c = 2**: Δ = 0 при a ≥ n − 2 доказуемо.
При a = n − 3 рассуждение сломается, и сломается конкретно: там впервые помещается
индуцированный C₅ (три вершины вне независимого множества плюс две внутри), а C₅ даёт
Δ = √5 − 2 > 0. Пятивершинный C₅ сам является примером: n = 5, a = 2 = n − 3.

**P8-4.** Полученная граница окажется **бесполезно далека** от интересной области: при
n = 12 доказуемо будет a ≥ 10, тогда как граница наследования a\* = 5. Разрыв в пять
слоёв. То есть подход от края даёт утверждение, но не дотягивается до того места, где
гипотеза нетривиальна.

**P8-5.** Литчек (8.d) снова не даст готовой сводимости. Ожидается, что оценки через
спектр дополнения и через дробное хроматическое число окажутся либо теми же, что уже
отвергнуты, либо неприменимыми по той же причине: они не параметризованы по α.

### Что считается подтверждением
Все пять сбылись.

### Что считается провалом
- P8-1: контрпример найден и подтверждён сертификатом ⇒ **гипотеза H7-S мертва**, и это
  главный возможный результат стадии.
- P8-2: поиск устойчиво останавливается ниже границы переноса ⇒ конус не локальный
  максимум, и надо понимать, почему.
- P8-3: c ≠ 2 в любую сторону.
- P8-4: n − c окажется ≤ a\* + 1 хоть при каком-то n ⇒ подход от края работает.
- P8-5: найдена оценка, дающая сводимость.

## 5. Чего не делается

- **Не запускать полный перебор n = 12** (164 059 830 476 связных графов, ≈ 490 суток).
- **Не объявлять «контрпримера нет»** без указания окрестности поиска, по §2.5.
- **Не менять определение слоя и границы наследования по ходу.**
- Не понижать планку: кандидат без точного сертификата результатом не считается.
- Не трогать закрытые Stage 6, 7, 7.1b и запечатанные предсказания.
