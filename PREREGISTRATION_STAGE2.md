# PREREGISTRATION — QUADC5, Stage 2 (закрыть восьмивершинную дыру)

Написано **до первого прогона высокоточного солвера**. После того как хэши из
`PREREGISTRATION_STAGE2.sha256` закоммичены, этот файл не редактируется; исправления
идут в `REPORT_STAGE2.md` датированными поправками.

Дата: **2026-08-20** (UTC). Репозиторий тот же, `.venv` тот же.

## 0. Отношение к печатям стадий 0 и 1

`PREREGISTRATION.md` и `PREREGISTRATION_STAGE1.md` не изменены. `SOURCES.md` дописан
(разделы S7–S9). Проверяемость прежних печатей сохраняется по префиксам:
префикс длиной в стадию 0 даёт `3a4fb60…` (значение из `PREREGISTRATION.sha256`),
префикс длиной в стадию 1 даёт `a2f0f12…` (значение из `PREREGISTRATION_STAGE1.sha256`).
Обе проверки воспроизводятся из git.

Стоящие правила действуют в редакции `PREREGISTRATION.md` §0 и не переписываются.

**Что известно на момент написания.** Всё содержимое стадий 0 и 1. В частности:
ϑ(`` GCQb`o ``) ≈ 3.4678437738, замкнутая форма неизвестна, и авторы исходной работы
объявили свою попытку PSLQ по 15 знакам ложноположительной (S1.9);
ϑ(`` ICQeR`[Mg ``) ≈ 3.6671550676, не поддалось 25 квадратичным полям при знаменателях
до 1500 (`REPORT_STAGE1.md` §1.d). **Ни одного прогона высокоточного солвера ещё не
сделано**; проверены только возможности mpmath и sympy на игрушечных входах (S7, S8).

---

## 1. Что считается результатом, а что нет

Единственный результат стадии — **проверенный сертификат**: примальная матрица,
дающая нижнюю границу, и двойственная, дающая верхнюю, обе точные, обе совпадающие с
корнем кандидата. Это записано отдельным пунктом, потому что планку задаёт чужая
ошибка: авторы n=8 угадывали минимальный многочлен по PSLQ и сами признали результат
ложноположительным (S1.9).

**Численное совпадение не является результатом ни при каком качестве.** Найденный
целочисленным соотношением многочлен — гипотеза для 2.c и ничего больше. Формулировки
вида «PSLQ уверенно даёт», «невязка 10⁻⁵⁰, сомнений нет» в `REPORT_STAGE2.md` не
появляются иначе как с явной пометкой «не доказательство».

**Отрицательный результат публикуем и заранее признан полноценным.** Если ни один
кандидат не проходит сертификацию, результатом стадии является граница вида
«минимальный многочлен ϑ не имеет степени ≤ d при высоте коэффициентов ≤ H», с явно
названными d и H, до которых поиск был исчерпывающим.

---

## 2. Блок 2.a — высокая точность

### 2.1. Метод и его обоснование

SDPA-GMP отвергнут по причинам, записанным в `SOURCES.md` S7 (нет GMP/MPFR, нет root,
три непроверенные зависимости ради входа поиска). Используется собственный уточнитель
на mpmath: старт с решения CLARABEL двойной точности, затем метод Ньютона—Гаусса на
системе условий оптимальности

- Tr X = 1;  X_ij = 0 для (i,j) ∈ E;  1ᵀX1 = t;
- B симметрична, B_ij = 1 при i = j и на нерёбрах, свободна на рёбрах;
- (t·I − B)·X = 0 (дополняющая нежёсткость).

Система переопределена; решается наименьшими квадратами с регуляризацией Тихонова
порядка 10^(−dps/2). При невырожденном решении сходимость квадратична.

### 2.2. ГЕЙТ 2.a — калибровка на четырёх уже доказанных случаях

Солвер обязан воспроизвести (S9): √5 (C₅, степень 2), 7cos(π/7)/(1+cos(π/7)) (C₇,
**степень 3**), 11/3 (n=9, степень 1), 3 + √2⁄2 (n=10, степень 2).

| # | требование | допуск |
|---|---|---|
| K1 | все четыре значения воспроизведены | ≥ 60 совпадающих знаков с точным значением |
| K2 | заявленная точность подтверждается удвоением рабочей | см. §2.3 |
| K3 | C₇ проходит **весь** конвейер 2.b + 2.c и получает сертификат над кубическим полем | точно |

**Солвер, не прошедший K1 или K2, к боевому прогону не допускается.** K3 — гейт для
аппарата высших степеней: если машинерия не переоткрывает уже известный кубический
случай, к неизвестным она не применяется.

### 2.3. Честное число верных знаков

Заявленной точности солвера не верим. Прогон делается дважды: при рабочей точности
`dps` и при `2·dps`. **Честным числом верных знаков объявляется длина совпадающего
префикса двух результатов минус 5 охранных знаков.** Именно это число, а не заявленное
солвером, идёт во все дальнейшие оценки и печатается в отчёте рядом с заявленным.

Целевая величина: **не менее 60 честных знаков** для обоих боевых графов. Если получить
их не удаётся, это записывается, и §2.b работает с тем, что есть, с пересчитанными
границами.

**Какая именно ошибка ловится гейтом 2.a.** (1) Ньютон, сошедшийся к точке, не
являющейся оптимумом (например, к седлу системы ККТ при неверно угаданном ранге X):
тогда значение разойдётся с известным на C₅ уже во втором знаке. (2) Молчаливая потеря
точности из-за того, что стартовое приближение задано в double и не перевычислено:
удвоение рабочей точности не изменит ответ, и K2 покажет ложно завышенное совпадение —
поэтому K2 требует не совпадения двух прогонов, а совпадения с **точным** значением на
калибровочных случаях, где оно известно.

---

## 3. Блок 2.b — поиск минимального многочлена

### 3.1. Правило запаса знаков, записанное ДО поиска

PSLQ по вектору (1, θ, θ², …, θ^d) из d+1 элементов при D верных десятичных знаках
надёжно находит соотношения с коэффициентами примерно до 10^(D/(d+1)). Принятое здесь
правило, с охраной:

> **Для степени d поиск ведётся с границей коэффициентов H = 10^B, где
> B = ⌊(D − 20)/(d + 1)⌋, и D — честное число знаков из §2.3.**

Двадцать знаков охраны выбраны так, чтобы вероятность случайного соотношения была
пренебрежимой: случайное соотношение с коэффициентами ≤ H даёт невязку порядка
H^(−(d+1)) = 10^(−(D−20)), то есть на 20 порядков хуже достижимой.

При D = 60 это даёт: d=2 → B ≤ 13; d=3 → B ≤ 10; d=4 → B ≤ 8; d=6 → B ≤ 5; d=8 → B ≤ 4.
Перебираются степени **d = 2, 3, 4, 6, 8**, как требует бриф.

### 3.2. Что считается кандидатом, а что шумом

**Кандидат** — соотношение, прошедшее все четыре проверки:
1. PSLQ вернул его при рабочей точности D;
2. **то же самое** соотношение возвращается при удвоенной точности 2·D и при границе
   коэффициентов, поднятой на десятичный порядок;
3. |p(θ̂)| < 10^(−D+5) при удвоенной точности;
4. p неприводим над ℚ, и θ̂ — его вещественный корень (проверяется изоляцией корней,
   а не подстановкой).

**Шум** — всё остальное, в частности: соотношение с коэффициентами, близкими к границе
H; соотношение, меняющееся при удвоении точности; приводимый многочлен, чей
неприводимый сомножитель с корнем θ̂ имеет меньшую степень (тогда кандидатом считается
сомножитель).

**Если кандидатов несколько — ведутся все** до 2.c, без предпочтения.

---

## 4. Блок 2.c — доказательство

### 4.1. Аппарат

`quadc5/numfield.py`: элементы поля ℚ(θ) степени d как векторы из d дробей в базисе
1, θ, …, θ^{d−1}; умножение — свёртка с приведением по минимальному многочлену;
обращение — расширенный алгоритм Евклида; **знак — точно**: сначала проверка на ноль по
координатам, затем сужение сертифицированного изолирующего интервала (S8) делением
пополам с точной рациональной оценкой знака p, пока рациональная интервальная оценка
значения не перестанет содержать ноль. Плавающая точка в этот слой не входит.

### 4.2. Что проверяется

Для каждого кандидата и каждого графа:

- **примал**: X симметрична; Tr X = 1; X_ij = 0 на всех рёбрах; 1ᵀX1 = θ;
  X ⪰ 0 **точно** ⇒ ϑ ≥ θ;
- **дуал**: B = 1 на диагонали и нерёбрах; (θ·I − B) ⪰ 0 **точно** ⇒ ϑ ≤ θ;
- обе границы совпадают ⇒ ϑ = θ.

**Проверка PSD двумя независимыми способами**, как требует бриф: (i) сдвиг Шура с
симметричным выбором ведущего элемента — тот же способ, что в стадиях 0 и 1;
(ii) все главные миноры (2ⁿ − 1 определителей, для n ≤ 10 это ≤ 1023) — независимая
реализация. Оба обязаны дать один вердикт; расхождение останавливает стадию.

### 4.3. Критерий закрытия и критерий незакрытия

Закрыто: обе границы точны и равны корню кандидата. Не закрыто: ни один кандидат не
прошёл — тогда печатается граница «нет минимального многочлена степени ≤ d при высоте
≤ H» с теми d и H, до которых поиск был исчерпывающим, и это объявляется результатом.

**Какая ошибка ловится здесь.** Опознание элементов X в поле ℚ(θ) при большой степени d
и большом знаменателе почти всегда «удаётся» численно — множество таких чисел плотно.
Ловушка снимается тем, что после опознания проверяются **точные** равенства (след,
нули на рёбрах, сумма) и **точная** PSD; ни одно из них не выполняется у случайно
подогнанной матрицы. Именно поэтому опознание не является частью доказательства, а
сертификат является.

---

## 5. Предсказания

| # | предсказание | обоснование | статус |
|---|---|---|---|
| P1 | ϑ(`` GCQb`o ``) иррационально, степень ≥ 3 | попытка авторов на 15 знаках провалилась; квадратичный поиск стадии 1 (6 полей, знаменатели ≤ 1500) к этому графу **не применялся**, поэтому степень 2 сначала проверяется отдельно и дёшево | СЛЕПОЕ |
| P2 | степень ϑ(`` GCQb`o ``) равна 3 или 4 | \|Aut\| = 8 и две орбиты вершин (листья {0,1,4,5}, хабы {2,3,6,7}); оптимальная X инвариантна относительно Aut, значит независимых параметров мало, и система ККТ низкой размерности | СЛЕПОЕ |
| P3 | степень ϑ(`` ICQeR`[Mg ``) **выше**, чем у `` GCQb`o `` | его оптимум почти вырожден относительно решения со значением 2/3 (ранг 5, разброс солверов 2.2e-7, `REPORT_STAGE1.md` §1.d) | СЛЕПОЕ |
| P4 | хотя бы один из двух графов закроется | — | СЛЕПОЕ |
| P5 | корреляции \|Aut\| со степенью поля не будет | ряд уже показал, что симметрия не связана с величиной зазора: C₇ вершинно-транзитивен и не содержит ни одного пятиугольника, а n=8 с \|Aut\| = 8 даёт самое трудное значение | СЛЕПОЕ |

P2 и P3 могут провалиться независимо друг от друга; провал любого из них результатом
не является и записывается как промах.

---

## 6. Блок 2.e — симметрия против степени поля

Для максимизаторов n = 5, 6, 7, 8, 9, 10 сопоставляются: порядок \|Aut\|, вершинная
транзитивность, степень поля, в котором лежит ϑ. Разведка; отрицательный ответ допустим
и заранее признан. Гипотеза до счёта не формулируется, кроме записанного P5.

---

## 7. Блок 2.f — чего не делается

Не n=11. Не общие теоремы о ряде. Ни при каких обстоятельствах численное совпадение не
подставляется вместо сертификата.

---

## 8. Порядок исполнения

```
SOURCES.md (S7–S9) + этот файл + SHA-256 + commit   ← до первого прогона солвера
   └─ 2.a высокая точность          ГЕЙТ 2.a (K1, K2, K3)
        └─ 2.b поиск многочлена      (кандидаты, не ответы)
             └─ 2.c сертификаты      ← единственное, что засчитывается
                  ├─ 2.d то же для n=10 ранга 2
                  └─ 2.e симметрия против степени
                       └─ REPORT_STAGE2.md
```
