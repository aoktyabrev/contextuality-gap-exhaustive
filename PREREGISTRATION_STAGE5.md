# PREREGISTRATION — QUADC5, Stage 5 (послойная структура по числу независимости)

Написано **до первого прогона стадии**. После того как хэши из
`PREREGISTRATION_STAGE5.sha256` закоммичены, файл не редактируется.

Дата: **2026-08-20** (UTC).

## 0. Что известно на момент написания

Всё содержимое стадий 0–4. Существенное для этой стадии:

- Δ_max по размерам: 0.2360680 (n=5), 0.2360680 (n=6), 0.3176672 (n=7), 0.4678437 (n=8),
  2/3 (n=9), 1/√2 (n=10);
- α максимизатора: 2 при n = 5, 6 и 3 при n = 7…10; в топ-10 α равен {2} при n = 5, 6,
  {2, 3} при n = 7, 8 и {3} при n = 9, 10 (Stage 4, блок 4.d);
- механизм из 4.c: у семейства C₇+K₃ число независимости прыгает с 3 на 4, и Δ
  обрушивается ровно до значения C₇ — накопленное преимущество съедается целым шагом α;
- распределение Δ атомарно: 61 % графов с зазором при n = 10 сидят на Δ = √5 − 2
  (Stage 4, блок 4.a);
- Δ_max(9) = 2/3 лежит вне носителя хвоста, подогнанного по остальной части
  девятивершинного распределения, устойчиво на всех порогах (Stage 4).

## 0.1. Отклонение, объявленное заранее: нужен дешёвый проход по α

Бриф говорит, что новых переборов не требуется. Это верно для n ≤ 9, но **не для n = 10**:
в репозитории лежат только графы с Δ > 0 (`n10_nonzero.csv.gz`), а таблица слоёв требует
числа графов и медианы ϑ **по всем** 11 716 571 графам. Поэтому делается один проход,
вычисляющий **только α** (без единого SDP): поток `geng`, декодирование, пакетная α.
По замерам Stage 1 это ≈ 0.18 мс на граф, то есть около пяти минут на семи ядрах.

Это не новый перебор в смысле 5.g — SDP не запускается, значения ϑ не пересчитываются,
берутся из уже посчитанных данных. Отклонение записано до счёта.

**Почему max ϑ в слое считается корректно и без этого прохода:** у графа с Δ = 0 имеем
ϑ = α ровно, поэтому такой граф не может быть максимумом своего слоя, если в слое есть
хоть один граф с Δ > 0. Максимум слоя поэтому берётся из `n10_nonzero.csv.gz`; проход по
α нужен только для счётчиков и медиан.

---

## 1. Определение слоя, фиксируется здесь и не меняется (5.g)

**Слой (n, a)** — множество связных неизоморфных графов на n вершинах с α(G) = a.
Величина слоя: **M(n, a) = max{ ϑ(G) : G в слое }**, и **D(n, a) = M(n, a) − a**.
Общий максимум: Δ_max(n) = max_a D(n, a).

Графы с α = 1 (полные) дают ϑ = 1 и D = 0; они входят в таблицу для полноты.

## 2. Блок 5.a — гейт согласованности

**ГЕЙТ:** max_a D(n, a) обязан **точно** воспроизвести известные Δ_max(n) для всех шести
размеров, и argmax обязан совпасть с известным максимизатором по коду graph6. Допуск —
τ_hi = 1e-7, как везде в проекте. Провал означает ошибку в разбиении: стоп, разбирательство.

**Какая ошибка этим ловится:** потеря графов при разбиении (например, если слой строится
по данным, где нет графов с Δ = 0, а слой целиком из таких состоит, его максимум будет
пропущен, а не равен a). Гейт это поймает на несовпадении argmax.

## 3. Предсказания и ожидания

| # | записано | обоснование | статус |
|---|---|---|---|
| L1 | гейт 5.a пройдёт | разбиение по α — точное целочисленное свойство | предсказание |
| L2 | слой a = 3 доминирует при n = 7…10; слой a = 2 доминирует при n = 5, 6 и упирается в потолок | из брифа; α = 2 означает, что дополнение свободно от треугольников, а это при росте n вынуждает граф к плотности | ожидание |
| L3 | **атом Δ = √5 − 2 расщепится по слоям**, и внутри слоёв доля крупнейшего атома окажется заметно ниже 61 % | значение √5 − 2 достигается графами с разными α (пятиугольник плюс что угодно, не мешающее); смешаны именно слои | предсказание |
| L4 | значение 2/3 **останется максимумом своего слоя** при n = 9, но его отрыв внутри слоя окажется существенно меньше, чем на фоне всех графов вперемешку | отрыв в Stage 4 мерился против распределения, куда свалены все α сразу | предсказание |
| L5 | слой a = 4 при n = 10 **приблизится** к слою a = 3, но не обгонит | иначе максимизатор n = 10 имел бы α = 4, а он имеет α = 3 | предсказание |

**L3 и L4 — главные.** L4 есть прямая проверка того, была ли «аномалия девятки»
артефактом смешивания слоёв; если он подтвердится, вопрос «почему 2/3 достижимо на
девяти вершинах» получает ответ вида «потому что сравнивали несравнимое».

**L4 может провалиться, и это будет содержательно:** если 2/3 остаётся выбросом и внутри
слоя a = 3, значит аномалия реальна и смешивание слоёв ни при чём.

## 4. Блок 5.b — характер роста слоёв

Для каждого a строится M(n, a) как функция n. **Форма не подгоняется заранее**: печатаются
значения и приращения, и вывод делается из них. Проверяются, но не предполагаются:
линейный рост, степенной, насыщение.

## 5. Блок 5.c — точки пересечения слоёв

Ищется, при каком n слой a обгоняет слой a − 1 по D(n, a), и насколько близки соседние
слои около перехода. Отдельно — расстояние между a = 3 и a = 4 при n = 10 и его динамика
по n.

## 6. Блок 5.d — предсказание на n = 11, записываемое до всякого счёта

Численное предсказание требует данных 5.a–5.c и поэтому не может стоять в этом файле.
Порядок такой, и он обязателен:

1. считаются 5.a–5.c;
2. предсказание для n = 11 — доминирующий слой и интервал для Δ_max(11) — пишется в
   **отдельный файл `PREDICTION_N11.md`**, хэшируется в `PREDICTION_N11.sha256` и
   коммитится **до того, как хоть один одиннадцативершинный граф будет посчитан**;
3. только после этого n = 11 может считаться — в другой стадии, не в этой (5.g).

Метод экстраполяции фиксируется здесь заранее, чтобы его нельзя было подобрать под ответ:
**по каждому слою a берутся значения M(n, a) при всех доступных n, оценивается приращение
M(n+1, a) − M(n, a) на последних двух-трёх точках, и прогноз на n = 11 строится как
линейная экстраполяция этого приращения с интервалом от минимального до максимального
приращения на этих точках.** Никакая другая форма не используется.

## 7. Блок 5.e — атомы против слоёв

Крупнейшие атомы распределения Δ раскладываются по a и по порождающей структуре.
Считается доля крупнейшего атома **внутри** каждого слоя и сравнивается с общей.

## 8. Блок 5.f — объясняет ли это девятку

Внутри слоя a = 3 при n = 9 строится то же, что в Stage 4: доля различных значений,
отрыв верхнего значения от следующих, и подгонка хвоста по различным значениям слоя с той
же сеткой порогов 0.50 / 0.80 / 0.90 / 0.95. Вердикт формулируется по **устойчивости на
всех порогах**, как в Stage 4, а не по одному удобному.

## 9. Чего не делается

Не n = 11 в этой стадии. Определение слоя не меняется. Теория не строится до гейта 5.a.
