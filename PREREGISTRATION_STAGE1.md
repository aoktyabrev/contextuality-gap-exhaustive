# PREREGISTRATION — QUADC5, Stage 1 (n = 10)

Написано **до первого сэмпла Stage 1**. После того как хэши из
`PREREGISTRATION_STAGE1.sha256` закоммичены, этот файл не редактируется; всё, что
придётся исправить, идёт в `REPORT.md` датированной поправкой, называющей, что именно
она меняет.

Дата: **2026-08-19** (UTC). Репозиторий тот же, `/home/artem/QUADC5`, тот же `.venv`.

## 0. Отношение к печати Stage 0

`PREREGISTRATION.md` не изменён. `SOURCES.md` **дописан** (разделы S5, S6) — этого
требует бриф Stage 1 («SOURCES.md продолжается»). Печать Stage 0 остаётся проверяемой:
префикс `SOURCES.md` длиной в стадию 0 побайтово совпадает с версией из коммита
`dc07d1f` и хэшируется в `3a4fb60b6e3814a8b0df4214ac79068577f4c78ce6471b70fb91810ee2f05a36`
— ровно то значение, что записано в `PREREGISTRATION.sha256`. Проверка воспроизводится
командой `git show dc07d1f:SOURCES.md | cmp - <(head -n $(git show dc07d1f:SOURCES.md | wc -l) SOURCES.md)`.
Новая печать `PREREGISTRATION_STAGE1.sha256` покрывает этот файл и `SOURCES.md` целиком.

Стоящие правила (0, калибровки, репозитория, kill, «тест, не способный провалиться,
вакуумен») действуют в редакции `PREREGISTRATION.md` §0 и здесь не переписываются.

**Что уже известно на момент написания.** Всё содержимое Stage 0: `REPORT.md`,
`results/`, включая максимум Δ(9) = 2/3 на `HCRbdO{` и точный сертификат для него.
Ни одного графа на 10 вершинах ещё не сгенерировано; `geng` собран и проверен только
на `-c 5 -u` (21 граф, S5.3).

**Отмена запрета 0.e.** Бриф Stage 0 запрещал n=10 оценкой «на два порядка больше».
Оценка была завышена: замер Stage 0 дал 382 с на 261 080 графов, откуда n=10 попадает
в десятки минут, а не в сутки. Запрет снят брифом Stage 1; причина отмены записана
здесь и повторяется в `REPORT.md`.

---

## 1. Допуски

Действуют допуски Stage 0 (`PREREGISTRATION.md` §2): τ_an = 1e-12, τ_hi = 1e-7,
τ_bulk = 1e-5, τ_gate = 1e-6. Добавляется:

| символ | значение | к чему | обоснование |
|---|---|---|---|
| τ_zero | 1e-6 | порог «Δ = 0» | не 1e-9 из брифа Stage 0: тот лежит ниже шума солверов. На n=8 и n=9 Δ двумодально с разрывом в 3–5 порядков (верх нулевого кластера 1.1e-07 и 9.0e-07, наименьший положительный зазор 0.0258 и 0.00366), и любой порог внутри разрыва даёт то же разбиение. **Предсказание: на n=10 разрыв сохранится, то есть верх нулевого кластера будет ниже 1e-5, а наименьший положительный зазор — выше 1e-4.** Если разрыв схлопнется, порог перестанет быть безразличным, и это отчитывается как отдельный результат. |
| τ_cert | точно | сертификаты 1.d | рациональная/алгебраическая арифметика, ноль допуска по определению |

---

## 2. Блок 1.a — объём и генерация

Генерация потоком через `build/nauty2_9_3/geng -c -l <n>`, без скачивания `graph10.g6`.
Ключ `-l` (каноническая разметка, S5.1) взят потому, что без него коды graph6
отражают порядок порождения, а не канон.

| # | предсказано | допуск | что означает провал |
|---|---|---|---|
| G1 | `geng -c 9 -u` даёт **261 080** | точно | генератор или ключи неверны |
| G2 | `geng -c 10 -u` даёт **11 716 571** [из брифа, UNVERIFIED, S6] | точно | либо бриф неверен, либо генератор |
| G3 | множество кодов от `geng -c -l 9` **совпадает как множество строк** с `sources/mckay_graph9c.g6` | точно | несовпадение канонизации |
| G4 | таблица Stage 0 воспроизводится: те же 261 080 строк с теми же α, и Δ в пределах τ_bulk | τ_bulk по Δ, точно по α | сдвиг конвейера между стадиями |

**ГЕЙТ 1.a:** провал G1 или G4 — стоп, 1.c не запускается.

G3 отделён от G4 намеренно и **не** входит в гейт. Причина записана до счёта:
совпадение классов изоморфизма гарантировано корректностью geng, а совпадение
**строк** зависит от того, той ли канонизацией пользовался McKay при выкладке
`graph9c.g6`. Если G3 провалится, а G4 пройдёт после сопоставления по изоморфизму —
это факт о канонизации, а не ошибка конвейера, и он так и отчитывается.

**Какая именно ошибка провалила бы эти тесты.**
1. Забыть `-c` — получится 12 005 168 графов на 9 вершинах вместо 261 080 (все, не
   только связные); G1 упадёт немедленно.
2. Забыть `-l` — G3 упадёт, G4 пройдёт. Именно поэтому они разделены.
3. Потерять часть потока при `res/mod`-разбиении (перепутать `-X`/`-x` между частями,
   S5.1) — сумма частей не сойдётся с `-u`-счётом; поэтому счёт по `-u` берётся
   отдельным прогоном, а не как сумма частей.
4. Читать stdout geng построчно без учёта строки-заголовка `>A`/`>Z` — лишние записи;
   ловится G1.

---

## 3. Блок 1.b — отсев

**F1** = совершенные графы через индуцированные нечётные дыры и антидыры (для n ≤ 10
это длины 5, 7, 9 в G и в Ḡ). **F2** = χ(Ḡ) = α, звуковый из процитированного
сэндвича α ≤ ϑ ≤ χ(Ḡ) (S1.2).

**Записано до счёта: F1 ⊆ F2 тождественно.** Совершенство влечёт χ(Ḡ) = ω(Ḡ) = α(G),
то есть каждый граф, снятый F1, снят и F2. Это подтвердилось на обоих размерах
Stage 0 точно (n=8: 7805 + 2814 = 10 619; n=9: 126 777 + 117 764 = 244 541).
Следовательно **связка F1 ∪ F2 = F2**, и в боевом прогоне применяется F2 —
не как отступление от брифа, а как та же самая связка, посчитанная дешевле:
F2 стоит 0.05 мс против 0.19 мс у F1 на n=10. F1 считается на выборке, ради
отчётных процентов и ради сверки с внешним `geng -P` (S5.2).

| # | предсказано | обоснование |
|---|---|---|
| B1 | F2 снимает **6–10 %** менее чем… то есть оставляет 6–10 % под SDP | n=8 оставил 4.48 %, n=9 — 6.33 %; доля растёт |
| B2 | ложных срабатываний F2 на выборке 10⁵ отсеянных: **0** | на n=8 и n=9 было ровно 0; звуковость выводится из сэндвича, а не из статистики |
| B3 | F1 ⊆ F2 без исключений на n=10 | вывод выше |
| B4 | счётчик `geng -c -P 10 -u` совпадёт с числом графов, которые наш F1 объявит совершенными | внешняя сверка распознавания |

**ГЕЙТ 1.b (формулировка брифа):** взять случайные 10⁵ отсеянных графов, прогнать
полный конвейер, доля ложных срабатываний должна быть того же порядка, что на n=9.
На n=9 она равна нулю, поэтому гейт читается как **ноль ложных срабатываний из 10⁵**;
любое ненулевое число — стоп и разбирательство поимённо.

**Какая ошибка это ловит.** Ошибка в `chromatic_number` в сторону занижения (например,
ранний выход при неполном переборе раскрасок) дала бы χ(Ḡ) = α там, где на самом деле
χ(Ḡ) > α, то есть ложное срабатывание с Δ > 0 — ровно то, что гейт измеряет. Ошибка
в сторону завышения безобидна: она лишь оставляет лишние графы под SDP.

---

## 4. Блок 1.c — перебор n=10 и оценка времени ДО запуска

Конвейер: `geng -c -l 10 r/mod` → декод graph6 → α точно → χ(Ĝ) → если χ(Ḡ) ≠ α, то
SDP → запись чанка. Сид `--seed 20260819`; перебор детерминирован, сид влияет только на
случайные тесты и на рестарты η_d. Одна команда: `bash runners/run_stage1.sh`.
Непрерывная запись чанками, прогон возобновляем.

**Оценка времени, записанная до запуска.** Замеры Stage 0 на n=9 (одноядерные):
декод 0.03 мс, α 0.06 мс, χ(Ḡ) 0.02 мс, F1 0.08 мс, SDP 5.07 мс; полный прогон
261 080 графов с SDP на всех — 382 с на 7 процессах. Масштабирование на n=10:
SDP ×(10/9)³ ≈ 1.37 → **7 мс**; χ(Ḡ) ×2.5 → 0.05 мс; α ×2 → 0.15 мс (пакетная форма);
декод 0.03 мс; F1 ×2.34 → 0.19 мс (в боевом прогоне не считается).

- фильтрующий проход по всем 11 716 571: 0.23 мс × 11.72e6 = 2 700 с одноядерно
  → **385 с на 7 процессах**;
- SDP на выживших (при B1 = 7.5 %, то есть ≈ 879 000 графов): 7 мс × 879e3 = 6 150 с
  → **880 с на 7 процессах**;
- генерация geng: **< 120 с**.

**Предсказание T1: полный прогон 1.c укладывается в 22 ± 10 минут на 7 процессах,
и заведомо менее 50 минут.** Это предсказание, а не оценка: оно проверяется секундомером
и печатается в `REPORT.md` рядом с фактом.

**Сохраняется на каждый граф:** graph6, n, число рёбер, α, ϑ, Δ, время решения, статус
солвера, первичная невязка, флаг отсева. **d\* на каждый граф не сохраняется** — см.
§7, отклонение 1.

**Контроль точности:** топ-100 по Δ пересчитывается вторым солвером (CLARABEL);
расхождение больше τ_hi разбирается поимённо, усреднение двух значений ϑ запрещено.
Фактически пересчитывается топ-1000, чтобы граница сотни сама не была артефактом.

---

## 5. Блок 1.d — точные сертификаты

Для топ-5 на n=10 и для ранга 2 на n=9 строятся и проверяются в точной арифметике:
примальный (X допустима, PSD, 1ᵀX1 = заявленное значение) и двойственный
(B = 1 на диагонали и нерёбрах, λ_max(B) = то же значение).

Метод, отработанный в Stage 0: численный X → распознавание знаменателя → точная
матрица → PSD через все главные миноры → двойственная B из условия дополняющей
нежёсткости (t·I − B)·M = 0 → проверка λ_max точно. Для алгебраических значений
поле расширяется (для ранга 2 на n=9 ожидается ℚ(√5), поскольку ϑ численно равно
(5+√5)/2 = 3.6180339887 при расхождении 1.2e-08).

**Предсказания:**

| # | предсказано | обоснование |
|---|---|---|
| E1 | сертификат для ранга 2 на n=9 строится, ϑ = (5+√5)/2 | численное совпадение на 1.2e-08 плюс рациональная структура, найденная у ранга 1 |
| E2 | из топ-5 на n=10 сертификат удастся построить **не для всех** | у ранга 1 на n=9 оптимальная X оказалась рациональной с знаменателем 99 и рангом 4; нет причины ждать этого от всех пяти |
| E3 | хотя бы для максимизатора n=10 сертификат построится | ϑ максимизатора на n=8 и n=9 оказывался «красивым» (2+√2 у Вагнера, 11/3 у n=9) |

**Запрет, повторённый явно.** Если замкнутая форма не даётся — записывается факт с
указанием, где именно застряло (не распознан знаменатель / матрица не PSD в точной
арифметике / двойственная система несовместна). **PSLQ-угадывание вместо доказательства
не подставляется** — это ровно та ошибка, которую авторы n=8 признали у себя (S1.9).
Численно указанное значение помечается «численно указано» и в сертификаты не идёт.

---

## 6. Блоки 1.e и 1.f — ряд и инварианты

### 6.1. Ряд (1.e)

В одну таблицу сводятся максимумы для **n = 5, 6, 7, 8, 9, 10**. n=6 добавлен сверх
брифа: 112 связных графов, стоит секунды, а без него ряд имеет дыру. Колонки: Δ_max,
структура максимизатора, \|E\|, d\*, точная форма (если получена), α, ϑ.

**ОЖИДАНИЯ (записаны как ожидания, не как предсказания; их провал результатом не
является — так сказано в брифе):**

| # | ожидание | обоснование |
|---|---|---|
| X1 | Δ_max(10) > 2/3 | ряд 0.236, 0.318, 0.468, 0.667 монотонен, отношения соседних ≈ 1.35–1.47 |
| X2 | Δ_max(10) порядка 0.9–1.0 | 0.667 × 1.4 ≈ 0.93 |
| X3 | точка Δ = 1 особая (ϑ = α + 1); **записать, попадает ли максимум на неё, и не подгонять** | из брифа |
| X4 | d\*(10) ≥ 4 | на n=9 у 49 из 50 топа d\* = 4, у максимизатора η₃ = α точно |

**Против чего X1–X2 могут провалиться, записано до счёта.** Ряд 5→9 включает переход
7→8, где сменился тип максимизатора (цикл → не цикл), и 8→9, где сменилась размерность
(d\* 3 → 4). Экстраполяция степенного роста по четырём точкам, из которых две —
структурно разные режимы, слабая. Кроме того Δ_max не обязан быть монотонным по n:
связность обязательна, а девятая и десятая вершины увеличивают и α, и ϑ.

### 6.2. Инварианты (1.f) — разведка, гипотеза заранее не формулируется

Для максимизаторов всех шести размеров считается:
- плотность \|E\| / C(n,2) и \|E\| / n;
- вершинная транзитивность и порядок группы автоморфизмов (`networkx`, полный перебор
  изоморфизмов — n ≤ 10 это допустимо);
- **разложимость «цикл + клика»**, операционализированная так, как она обнаружилась на
  n=9: вершины разбиваются по степени на множество L наименьшей степени и множество H
  наибольшей; проверяется, индуцирует ли L цикл, индуцирует ли H клику, и по какому
  правилу H прикреплено к L. Для n=9 это дало C₆ + K₃ с прикреплением каждой вершины
  клики к антиподальной паре. Проверяется у топ-10 каждого размера, а не только у
  максимизатора;
- d\* и рациональность ϑ (последняя — по факту построения сертификата, а не по PSLQ).

**Отрицательный ответ допустим и заранее признан результатом:** если ни один инвариант
не окажется устойчивым по n, так и пишется.

---

## 7. Отклонения от брифа, объявленные заранее

1. **d\* не сохраняется для каждого графа.** Бриф 1.c требует его в списке полей.
   η_d — невыпуклая задача, и в Stage 0 её честный прогон стоил ≈ 9 с на граф (300
   рестартов, d = 2…5). Для 11.7 млн графов это ≈ 30 000 процессор-часов — вычисление,
   которого не существует. d\* считается для **топ-100** по Δ на n=10 и для
   максимизаторов всех размеров; в CSV полного перебора колонка d\* остаётся пустой.
   Отклонение объявлено до счёта, а не после того, как стало неудобно.
2. **В боевом отсеве применяется F2, а не F1 ∪ F2** — они тождественны как фильтры,
   вывод в §3. F1 считается на выборке и для сверки с `geng -P`.
3. **Добавлен n=6** в ряд 1.e.
4. **Пересчитывается топ-1000, а не топ-100** вторым солвером.
5. `graph10.g6` не скачивается — этого требует бриф; но `graph9c.g6` из Stage 0
   используется как эталон для гейта G3/G4.

## 8. Порядок исполнения

```
SOURCES.md (S5,S6) + этот файл + SHA-256 + commit   ← до первого сэмпла Stage 1
   └─ 1.a генератор            ГЕЙТ 1.a (G1, G4)
        └─ 1.b отсев           ГЕЙТ 1.b (0 ложных срабатываний из 10^5)
             └─ 1.c перебор n=10
                  ├─ 1.d сертификаты (плюс закрытие нити n=9 ранг 2)
                  ├─ 1.e ряд по n = 5,6,7,8,9,10
                  └─ 1.f инварианты
                       └─ REPORT.md
```
